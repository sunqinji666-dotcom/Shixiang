# 拾响 1.0.0 · Build 111

## Build 111 测试版变更

- 赞助铜钱改为单一 Button 点击链路，移除与外层按钮争抢事件的内部拖拽手势；每次点击都可靠显示微信二维码。
- 铜钱按六级逐次缩小，第 6 次点击后永久消失；状态随本机用户偏好保存，重启和覆盖更新不会恢复，只有没有旧偏好的全新安装环境会重新出现。固定点击区域与品牌栏布局不随视觉尺寸变化。
- 赞助二维码从标题栏附着式 Sheet 改为主窗口内部固定居中的浮层，不跟随铜钱尺寸或标题栏状态漂移。
- 快速点击不再穿透到融合标题栏触发窗口缩放，规避崩溃日志中记录的标题栏双击缩放与 SwiftUI 列表销毁冲突。

## Build 111 验证记录

- 铜钱六级递减、永久偏好键与边界专项测试通过；实机已确认固定居中二维码可以显示，连续点击后铜钱在第 6 次消失。
- 全套自动测试共 144 项：143 项通过，1 项按当前系统能力正常跳过，零失败。
- Release App 为 Apple Silicon `arm64`，最低系统为 macOS 14.0，深层严格签名校验通过。
- App 主可执行文件 SHA-256：`6f9db0e042e90c55db172a0e12c2efb9225f11044c25b3316e9d32b3013c1dff`。
- DMG SHA-256：`17818ce885ea5fe3e52e7c99340c01a5a0f21405e5b79e70c2d9276f7551ccbb`。

## Build 110 正式候选变更

- 主界面赞助铜钱重做为多层铜金材质，加入克制的流光、悬停抬起和即时按压反馈；系统开启“减少动态效果”时自动停用装饰性运动。
- “关于拾响”明确展示并可点击访问 `shixiang.jack-sun.com`。
- 快速使用引导新增“领取 5 万+ 音效库”入口；真实空资料库引导仍可在官网下载附加库或导入自己的音效包。
- 官网桌面导航统一为相同高度与中心线，教程入口保持突出但不再改变整排对齐。

## Build 110 验证记录

- Release App 构建、141 项自动测试（1 项按系统条件正常跳过）、arm64 架构、macOS 14.0 最低系统与深层严格签名校验通过。
- 实机确认官网链接为可点击系统链接，快速引导音效库入口可见；官网导航桌面端各项高度与中心线一致。
- App 主可执行文件 SHA-256：`3c65ae5dc94c4d4a24dfe2c085f72606c6d70a9a7d0151a72419ccd8ec024be5`。
- DMG SHA-256：`708e59d0e441e48bae9ba9b48aa85806af461a2503b77a76ebaf15a648d01439`。

## Build 109 正式候选变更

- 最低系统版本调整为 macOS 14.0；资料库、普通搜索、试听、整理、声音工作台和拖入剪辑软件可在 macOS 14.0+ 使用。
- 本地 AI 自然语言搜索单独要求 macOS 26.2+；低版本点击 AI 会显示明确提示，不会影响其他功能。

## Build 109 验证记录

- Release App 构建、arm64 架构、macOS 14.0 最低系统与深层严格签名校验通过。
- DMG 文件系统校验及挂载后版本、官网入口、内置 AI 运行时和赞助二维码资源检查通过。
- 自动测试通过：AudioTests 25/25、FirstLibrarySetupTests 1/1、SystemCompatibilityTests 1/1、AISearchRankingTests 10/10，零失败。
- App 主可执行文件 SHA-256：`df08b18d864073953a1060b5d364e6b67ff7af3f7270b503536bb6a244c579fc`。
- DMG SHA-256：`fbb5c3c75797211cac26669ee76ea5a4162a64584b62126cb020a8cf0211d59a`。

## Build 108 正式候选变更

- 左上角拾响图标与品牌文字成为官方网站入口，点击打开 `https://shixiang.jack-sun.com`。
- 品牌栏新增金色方孔铜钱赞助按钮，点击显示独立微信扫码赞助卡片；赞助完全自愿，不影响免费、开源版本的任何功能。
- 微信收款二维码根据原始有效内容重新生成，使用高纠错等级、象牙白背景与中心 Jacksun 头像金色圆章；二维码本体不经过 AI 重绘，并通过系统扫码识别验证。
- 关于页面官方网站同步为拾响独立产品域名。
- 产品站改为纯代码设计语言：声纹、音效列表、波形、频谱、本地 AI 核心、安装流程与轻量音符动效均由 HTML/CSS 绘制，不使用概念图或软件截图冒充界面。
- 产品站百度网盘下载按钮会先自动复制提取码 `jack`，提示复制成功，再打开正式下载链接。

## Build 108 验证记录

- Release App 构建与深层严格签名校验通过；DMG 文件系统校验及挂载后 Build 108、arm64、官网入口、内置 AI 运行时和赞助二维码资源检查通过。
- 自动测试通过：AudioTests 25/25、FirstLibrarySetupTests 1/1，零失败。

## 1.0.0 正式版变更

- 确立拾响 1.0 正式版品牌、统一应用图标、启动页与“关于拾响”页面，并加入拾响官网 `shixiang.jack-sun.com`。
- 首次进入时资料库在后台恢复，文件列表区域原位显示准备进度；结果快照建立前不再闪现“没有声音”等错误空状态。
- 每次启动默认进入“全部声音”，左侧智能分类、音效包与文件夹默认折叠，为新用户提供干净一致的第一眼体验。
- AI 搜索与拖入剪辑软件提示改为悬浮式一次性气泡，不参与页面布局；用户主动关闭后不再出现。
- 播放栏 64 段频谱取消播放/停止形变动画，开始与结束即时响应；复杂音频在真实 FFT 分析完成前由实时播放电平先行驱动画面，长音频分析帧数和刷新频率保持有界。
- 本地 AI 搜索运行时、Python 标准库与 Qwen 模型继续内置在 App 中，目标电脑无需另装 Python、Homebrew、Xcode 或联网下载模型。
- DMG 改为图形化一步安装：打开后将“拾响.app”拖入“Applications”，画面明确标注 Apple Silicon 与 macOS 26.2 最低要求。

## 1.0.0 验证记录

- 完整产品测试通过：CoreTests 87/87、AudioTests 25/25、KeyAnalyzerTests 5/5；PitchShiftTests 10 项执行，其中 1 项因当前系统具备变调组件而正常跳过，0 失败。
- Release App 为 Apple Silicon `arm64`，最低系统版本为 macOS 26.2；版本、构建号、README、发布说明与 DMG 文件名均为 `1.0.0 · Build 107`。
- App 深层严格签名校验通过，签名身份为 `Xiangqi Pilot Local Signing`；可执行文件 SHA-256：`c5d2cdf5b13156013f22f6585ab61bbd682d0ed5cbd5497dff528e11c2a58ac0`。
- 图形化 Finder 安装窗口已经真实挂载并验收：拾响与 Applications 图标可直接拖拽，标题、箭头、本地 AI 说明、`macOS 26.2` 最低要求与官网均在第一屏完整可见。
- DMG 文件系统校验与 SHA-256 校验通过；DMG SHA-256：`f2a860108dc36ab7872d8950763dfdf3903edeab615bfe99a5a7b039c15598bb`。
- 从已挂载 DMG 内部直接启动内置 Python + Qwen 模型完成自然语言搜索烟测，返回受控关键词；模型运行后 App 签名仍保持有效。
- 源码、发布文档与 App 包内高风险敏感模式扫描通过；App 不包含当前电脑的用户绝对路径、音效库、旧索引、凭证或恢复数据。

## Build 106 变更

- 撤回 Build 105 左右两张独立圆角卡片的拼接感，改成一个带 8pt 外边距的统一圆角工作台；侧栏、声音列表与底部播放器共享同一外层底板，只由原生分栏和 hairline 分隔功能区域。
- 重做侧栏信息层级：顶层、分支和叶子分别使用 32/29/27pt 行高、递减字号与嵌套金色树干；每深入一级增加稳定缩进，当前叶子的选中胶囊也随层级内收。
- 修正智能目录多重选中：父级和中间分支只显示暖金路径提示，只有当前真正打开的范围拥有紫蓝选中面和 VoiceOver 选中语义。
- 侧栏默认宽度提升为 288pt，可在 250–380pt 内原生调整；右侧安全区增至 18pt，五位数使用“3.13万”等紧凑显示避开系统滚动条，VoiceOver 和主标题仍提供精确数量。
- 数量不再机械固定到侧栏最右缘，改为在目录名称后保持 10pt 最小间距，剩余弹性空间留到数量右侧；短名称不会形成大段无意义空白，长名称仍会安全截断。
- 启动页改为约两秒的拾响品牌开屏：以大图标和底部流动光带呈现身份，后台预热本地资源库后直接进入工作台；左上角品牌副标题已收起。
- 底部播放器重新整理频谱、进度条与时间码的纵向关系：时间码与进度条同排右对齐，频谱高度增加，播放器底部空间更完整。
- 底部播放器移除运输控制与音量两侧的独立卡片边界，仅保留中央可拖声音身份区；整体从多卡片仪表板收敛为连续的电影声音工作台。
- 保留 Build 105 的原生动效：层级展开采用可中断低回弹弹簧，唯一选中状态使用 130ms ease-out；窗口缩放、波形、进度与音量继续禁用补间并 1:1 跟手，兼容“减少动态效果”。

## Build 106 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁待本轮完成后补录 SHA-256。

## Build 105 变更

- 参考 AiMaMi 的 inset 桌面布局，将拾响左侧资料库和主声音工作区改成两个独立圆角舞台；侧栏保留 8pt 外边距，主工作区保留 6–8pt 间隙，不再贴住窗口边缘。
- 主工具栏、声音列表和底部播放器保持在同一个完整内容舞台中，侧栏品牌、目录和导入入口保持在另一个舞台中；两区使用统一 12pt 圆角和轻量 hairline 边界。
- 只借鉴 AiMaMi 的空间结构，不引入 Tauri、React、DOM 动画或 WebView 合成层；本地音频、窗口、拖放、SQLite 和辅助功能继续使用 SwiftUI/AppKit/AVFoundation。
- 新增原生动效节奏：侧栏选中状态 140ms 淡化，智能集合和音效包展开使用 0.24s、0.92 阻尼的可中断弹簧，主标题与结果数量使用 160ms 内容过渡；“减少动态效果”开启时自动停用非必要动效。
- 窗口 live resize、波形拖动、播放器进度和音量继续 1:1 跟手，不经过补间动画；高级感来自状态变化的连续性，而不是给高频输入增加延迟。

## Build 105 验证记录

- Debug 编译通过；完整产品检查通过：CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Release App、Build 105 DMG、本地签名、版本一致性与 SHA-256 发布门禁全部通过。可执行文件 SHA-256：`c6fc7ece2f547012d67680f6c7ce76750d237b8e095e9cd6e5a58747e3f6a389`；DMG SHA-256：`1d901dcb36f7628f74b074ad06ad5d8e77f110b0ff6d645e0d9eb1df732e21fa`。

## Build 104 变更

- 修复 Build 103 侧栏内容贴边：滚动内容使用 SwiftUI 原生 `contentMargins` 加入 10pt 水平、4pt 垂直安全区，展开箭头、选中指示和文字均与窗口边缘保持稳定距离。
- 侧栏宽度从 180–280pt 提升为 220–320pt，理想值 245pt；两级智能集合展开后仍保留标题和数量列的可读空间。
- 侧栏惰性布局行间加入 2pt 间距，降低密集层级的视觉拥挤，不引入额外动画或高成本绘制。

## Build 104 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 104 可执行文件 SHA-256：`11b597c9221344c27d97d2dc1bc155515437ebb54bb348e512c27bcfc3d2bf9d`；DMG SHA-256：`b7f2f2c89537fdeb08d39aec588c1018fc384676697cf0b48f22293a6fc42856`；`codesign --verify --deep --strict` 通过。

## Build 103 变更

- 修复侧栏真实渲染残影：移除 `List(selection:)` 的 macOS 虚拟化选中绘制层，改用 SwiftUI 原生 `ScrollView + LazyVStack`；仍由系统 NSScrollView 承载滚动，展开分支保持惰性创建。
- 侧栏保留手工选中语义、VoiceOver 标签、固定 30pt 行高和数量列约束；“反转与回吸”“极短/中等时长”等相邻行不再互相覆盖。
- 窗口 live resize 继续采用 AppKit `.duringViewResize`，不启用不安全的内容复用，波形维持低成本路径。

## Build 103 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 103 可执行文件 SHA-256：`f34956560e4b7360065785dc367c755346f56c9e1496569043661ab9965f17bd`；DMG SHA-256：`8ae70e1223293e0cd3ab15c9161d90a43cb5d887f0bf03084286e431fb6e41fd`；`codesign --verify --deep --strict` 通过。

## Build 102 变更

- 修复侧栏在窗口缩放后出现旧行残影、选中背景重叠和标题/数量互相覆盖的问题：不再对 SwiftUI 虚拟化 `List` 启用 AppKit 内容保留复用，交由系统正确重绘 hosting view。
- 侧栏行统一为 30pt 高度，标题采用尾部截断，数量列保持至少 34pt 的右对齐空间，列表行背景和分隔线交给原生透明配置；选中状态只保留一套视觉和辅助功能语义。
- 保留 AppKit `.duringViewResize`、波形轻量绘制和全部边缘/四角 live-resize 监听，避免为修复视觉而回退整体性能架构。

## Build 102 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 102 可执行文件 SHA-256：`af2cd2f69d24058f36aab1a6727dd9bbd8a18ea0a778466b90cc97adb05d0e22`；DMG SHA-256：`bd50edf948f160e3fd3e6307910d0dd33503e34c78222926d32494e7396be600`；`codesign --verify --deep --strict` 通过。

## Build 101 变更

- 窗口 live resize 明确启用 `NSWindow.preservesContentDuringLiveResize`，由窗口服务器保留未变化内容；不再强制启用异步 backing-layer 合成，减少 resize 时的撕裂或花屏风险。
- 保留原界面树和 `.duringViewResize` 正确重绘策略；所有边和四角仍由同一个 `NSWindow` 生命周期监听覆盖。

## Build 101 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 101 可执行文件 SHA-256：`36beaf74710fd551aa06fe66185804e21f439e3d744db588236d13eb8f7b7595`；DMG SHA-256：`5c9b385579ef5c06b461933722674f56627ea2de52886a7a7825fb50f31635f4`；`codesign --verify --deep --strict` 通过。

## Build 100 变更

- 撤回 Build 99 的窗口级轻量舞台切换。缩放过程中不再移除或重建 `NavigationSplitView`、列表、播放器和工作台，避免 SwiftUI 内容树切换造成花屏。
- 所有边缘和四个角仍统一监听 macOS `NSWindow` live-resize 生命周期；窗口内容保持连续，只有波形使用已有的轻量占位绘制路径。
- 将窗口内容的 `layerContentsRedrawPolicy` 设置为 AppKit 原生 `.duringViewResize`，并允许已有 backing layer 异步绘制；不创建额外图层、不改变源音频、不改变用户资料库。

## Build 100 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 100 可执行文件 SHA-256：`36a7ae622efdfda5b5fd5bb0d5efd539bc4370ec15dacc9aa3467a0cd8e217e9`；DMG SHA-256：`e6c0a2bd3998eb77b8dfe7466d0df771368a7991997c40a9707e9160a4338005`；`codesign --verify --deep --strict` 通过。

## Build 99 变更

- 修正窗口缩放优化范围：现在监听整个 macOS `NSWindow` 的 live-resize 生命周期，左边、右边、上边、下边以及四个角的拖拽都走同一条轻量路径。
- 缩放进行中移除重型 `NavigationSplitView`、资料库列表、工作台和波形树，显示保持品牌视觉的轻量舞台；松开鼠标后恢复完整内容，避免每个窗口几何事件触发数万条状态和路径重算。
- 窗口监听宿主改为非零尺寸且不可交互，避免 SwiftUI 在 live resize 中裁剪零尺寸背景导致通知丢失。

## Build 99 验证记录

- Debug 编译通过；完整产品检查、Release App、DMG 与发布门禁全部通过。CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Build 99 可执行文件 SHA-256：`d991ddc0aab356bcce1ab92ad093b755c6d5db0468350fdaf03ebdce7973532b`；DMG SHA-256：`e5dfda8c9dd64633d4c628dcbbb3c4221b01225ae513c177153eceb1d7857790`；`codesign --verify --deep --strict` 通过。

## Build 98 变更

- 针对 macOS 窗口右下角 live resize 优化：调整窗口期间不再为每个可见声音重算完整波形路径，当前播放波形也暂停 60Hz 动态时间线，改用轻量占位线保持布局反馈。
- 监听窗口开始/结束 live resize 两个边界；松手后自动恢复完整波形与播放进度，原始音频和试听状态不受影响。

## Build 98 验证记录

- Debug 编译通过；完整产品检查覆盖 CoreTests、AudioTests、KeyAnalyzerTests、PitchShiftTests、Release 构建、DMG 打包与发布门禁；原始音频仍只读。
- Build 98 完整产品检查：CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败；可执行文件 SHA-256：`c9d59f6aa0abec6d4db33490b83fda8b2a8f36adb0686ed5d97d850980f09a79`；DMG SHA-256：`5e8f1f52d5e613bbb280386a01fb50d1be7a304a21ab6986cc2b8fe411a31289`。

## Build 97 变更

- 工作台宽度/高度现在只在拖拽结束时保存；拖拽进行时只更新内存状态且显式关闭动画，避免高频 `UserDefaults` 写入或隐式补间影响指针跟手。
- 重启后恢复上一次松手确定的工作台尺寸，宽屏保持右侧面板宽度、窄屏保持底部抽屉高度。

## Build 97 验证记录

- 完整产品检查覆盖 CoreTests、AudioTests、KeyAnalyzerTests、PitchShiftTests、Release 构建、DMG 打包与发布门禁；原始音频仍只读。
- Build 97 完整产品检查：CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败；可执行文件 SHA-256：`cf339f48e6c11652ecf0ddbef40a761b1e96f98815e404af96eba7012730a21f`；DMG SHA-256：`108c2fee15a445a43f4716bb694dc9b9996a14de9ba3f7a0fb8d5058e91e5381`。

## Build 96 变更

- 导入或刷新单个音效包时，SQLite 现在只替换该包的声音行、FTS 原文检索、中文字符索引、语义概念、相似系列和对应智能信息；三万多条已存在声音不再随新包导入而整库重写。
- 单包索引替换与计数状态在同一个 SQLite 事务中提交，搜索要么看见旧包完整索引、要么看见新包完整索引，不会出现半更新结果。
- 新增回归：替换一个中文命名的转场包后，旧检索词立即失效、新检索词立即命中，另一音效包的中文检索结果保持可用。

## Build 96 验证记录

- 完整产品检查覆盖 CoreTests、AudioTests、KeyAnalyzerTests、PitchShiftTests、Release 构建、DMG 打包与发布门禁；原始音频仍只读。
- Build 96 完整产品检查：CoreTests 77/77、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败；可执行文件 SHA-256：`fe3f4be582fae4c7909f0092a3fe4fec410a502429cc9a4d35ea2c41185eb25c`；DMG SHA-256：`74c2c5a3ad55f9f47763fbe3b46c8b84f5b50bf43263db813b7cc1a5818c1df4`。

## Build 95 变更

- 声音工作台新增可抓取边缘：宽窗口左右拖动调整工作台宽度，窄窗口上下拖动调整底部抽屉高度；调整过程中关闭隐式动画，保证指针与面板 1:1 同步、可随时反向。
- 导入或刷新音效包时，SQLite 的中文字符检索索引与 FTS/语义行在同一次结构性事务中生成，取消原先写完 FTS 后再次遍历整库的重复工作。

## Build 95 验证记录

- 完整产品检查覆盖 CoreTests、AudioTests、KeyAnalyzerTests、PitchShiftTests、Release 构建、DMG 打包与发布门禁；原始音频仍只读。
- Build 95 完整产品检查：CoreTests 76/76、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败；可执行文件 SHA-256：`bf6b063df2ed0a36e7ce827c4ec3408a314f3ac669ede82d51b2a9a4459961df`；DMG SHA-256：`4c57a23d28ef0b322d6ef80dc819cde7c9a018d675dafcbdfa02011555a514ec`。

## Build 94 变更

- 声音工作台加入真正的响应式断点：宽窗口保留右侧浮层，窄窗口（小于 1,180px）自动改为底部抽屉，给中间波形、文件名、时长和格式完整的横向空间。
- 窄窗口工作台使用独立圆角、边界和阴影，仍可滚动分析、编辑标签、变调和导出；关闭动画保持一致。

## Build 94 验证记录

- 代码静态检查与 SwiftUI Release 构建通过；完整产品检查继续覆盖 CoreTests、AudioTests、KeyAnalyzerTests、PitchShiftTests、DMG 打包与发布门禁。
- Build 94 完整产品检查：CoreTests 76/76、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败；Release App 与 DMG 已完成签名、版本一致性和 SHA-256 门禁。可执行文件 SHA-256：`5bff48b4da6d4bb6d4e9529b0b47811cf1682eee4d1f58b61cb6b60227136ea4`；DMG SHA-256：`05d56d5cc7b7a9c53a1b6c1caa87906d797e4b35fc668bf82acfce4a9b9b2faf`。
- 本轮电脑控制服务在重启 Build 94 时返回窗口超时，未把这次桌面服务瞬态冒充为应用崩溃或新的 GUI 通过证据；Build 92 的 31,259 条本地资料库与停止播放走查证据继续有效。
- 原始音频仍只读；本批没有移动、改名、复制或写入任何源音频。

## Build 93 变更

- 声音工作台从固定第三栏改为右侧自适应浮层：中央音效列表不再被工作台强行挤窄，工作台宽度按窗口宽度的 30% 在 280–380px 之间调整。
- 左侧目录列同步收窄到 180–280px，窄窗口优先给文件名、波形、时长和格式保留可读空间，宽窗口仍可自然拉宽目录。
- 浮层带有独立边界、阴影和顺滑进出过渡；窗口变窄时仍保留主列表的文件名、波形、时长和格式可读性。

## Build 92 变更

- 播放完成或明确停止后，当前缩略图的辅助状态从误导性的 `0%` 统一恢复为“尚未播放”；真正播放中或暂停在中间位置时仍准确读出百分比。
- Build 92 完整产品检查通过：CoreTests 76/76、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Release App 与 Build 92 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `5dae2bf60dee9ef63ee4eeed533373df1c75bd5de9007d11b8d6b49d0abb701a`；DMG `5a6ae4a4efe237d3bd75a26b823f361fb1536522812c7decfab7518f1b5d395f`。
- 电脑插件真实走查 Build 92：进入本地资料库显示 31,259 条声音，播放 `1_大号.mp3` 后显示 5%，点击停止后该行恢复“尚未播放”，播放器回到播放按钮与 `0:00 / 0:01`，列表恢复顶部。
- 原始音频仍只读；本批没有移动、改名、复制或写入任何源音频。

## Build 91 验证记录

- 移除音效列表的悬停自动试听路径；鼠标移入、移动和停留都不会启动或替换播放器，只有点击播放按钮、点击波形或按住波形拖动才会试听。
- 保留可见行的静默解码预热，用于降低明确点击后的起播延迟；预热不触碰主播放器，也不产生声音。
- 完整回归：CoreTests 76/76、AudioTests 24/24、KeyAnalyzerTests 5/5、PitchShiftTests 10 项执行（1 项因当前系统具备变调组件而正常跳过）、0 失败。
- Release App 与 Build 91 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `09d3a1de2723cfc9089f5a9cec1f4491c86db59c1ff9c969be423fd806ef6725`；DMG `d479f605c7231e3beea8e49fbc40cd6cc92efd0c71e4ad146e92cd55c7a07f2b`。
- Build 91 通过电脑插件重新启动并读取真实本地资料库 31,259 条；试听停止后播放器回到播放按钮与 `0:00 / 0:02`，没有继续播放。电脑控制服务一次短暂返回 `noWindowsAvailable` 后可恢复读取，本批不把该服务瞬态冒充为应用崩溃。
- 原始音频仍只读；本批没有移动、改名、复制或写入任何源音频。

## 本批完成

- 音量滑块拖动期间只更新内存和正在播放的解码器，用户偏好改为在松手或控件退出时一次提交；不再以鼠标采样频率持续写入 `UserDefaults`。
- 快速连续选择声音时，工作区恢复状态改为 280 毫秒合并写入；选中与起播保持立即响应，连续试听不再为每次点击同步 JSON 编码和持久化。
- 播放波形拆分为静态灰色底图与 60 Hz 动态高亮层；底图、热缓存查询、载入任务和完整路径描边不再随播放进度每帧重复执行。
- 播放器整秒时间文本从 30 Hz 降为 5 Hz，进度滑块和波形仍保持 60 Hz；减少无可见变化的主线程排版，同时保留丝滑进度。

## 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：10 项执行、1 项因当前系统具备变调组件而正常跳过、0 失败。
- 最终 Release App 已完成真实启动与模式入口走查，空闲约 85 秒后进程 CPU 取样为 0.0%、物理内存约 0.4%，系统诊断目录没有生成拾响崩溃报告。
- 本批的高频优化由代码隔离与完整回归验证：音量拖动期间不再写偏好，快速选音只在 280 毫秒稳定后提交工作区状态，播放 Timeline 只重绘动态高亮层；桌面控制焦点受当前工具会话影响，本批不把不稳定的自动点击冒充为新的鼠标性能量化。
- Release App 与 Build 90 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `cb5f64288c9e8f2ccb89660a0c279646b1f32392d50d684f5adc2166a2f9a871`；DMG `d46653e245dd54a50adc1e67190556c1d6d991c26d98ac7e845be74d6555909f`。
- 本批只读取界面与本地索引状态，没有移动、改名、复制或写入原始音频；正式销售层、Developer ID 与 Apple 公证仍保持明确外部边界。

## Build 89 记录

## 本批完成

- 同步 `Docs/PRODUCT_READINESS.md` 与 `Docs/RELEASE_READINESS.md` 到 Build 88 的真实实现、测试、实机走查和归档证据，避免发布矩阵滞后于可运行产品。
- 明确当前唯一外部边界仍是 Developer ID/Apple 公证，以及孙哥明确排除的正式销售体系；本地版保持不联网、原始音频只读。

## 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：10 项执行、1 项因当前系统具备变调组件而正常跳过、0 失败。
- 最终 Release App 已完成真实启动与模式入口走查，“本地音效库 / 在线音效库”分流、在线版“尚未开发”状态和电影诗意视觉均正常；Build 88 已记录同一真实资料库 31,259 条启动与工作台证据，本批未把受限的桌面自动点击冒充为新的资料库鼠标验收。
- Release App 与 Build 89 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `20c89696d39dd63363e18776220b6d97d9b1e1d9dc2e769a05c1470942a90699`；DMG `ba4dc00c415e0703c2e3dd6d721ae279851a4267c751dd910cf6447060722dea`。
- 本批只读取界面与本地索引状态，没有移动、改名、复制或写入原始音频；正式销售层、Developer ID 与 Apple 公证仍保持明确外部边界。

## Build 88 记录

### 本批完成

- 声音工作台切换到新声音时，变调半音数和原音/处理音模式恢复到每条声音的默认状态，不再默默沿用上一条素材的创作参数。
- 用户的试听音量偏好继续保留；工作台只重置源声音相关的试听状态、加载文件和错误信息。
- 新增工作台切换回归：从 +7 半音、原音模式切换后恢复为 0 半音、处理音模式，同时保持 42% 试听音量。

### 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：10 项执行、1 项因当前系统具备变调组件而正常跳过、0 失败。
- 最终 Release App 已完成本地入口、31,259 条资料库启动和声音工作台入口走查；变调控制器的 per-sound 状态重置、错误隔离与原音/处理音入口由定向与完整回归覆盖。
- 计算机控制服务在工作台走查后短暂返回不完整无障碍树，但应用进程仍在运行且未生成崩溃报告；本批不把这次控制服务瞬态误报为应用崩溃，自动化产品门禁保持全绿。
- 应用已退出，工作区恢复为“全部声音”；本批只进行只读界面走查，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 88 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `56a3e61c9632107d993ffe88a17995f55cfc24f5369fb54f740e7766b5a1e00e`；DMG `d67ad9d23378c6dedeb236bd366de1006ef4864cd6b822609b73c2424a2c81c5`。

## Build 87 记录

### 本批完成

- 变调试听错误现在绑定具体源 URL；损坏或无法解码的声音失败后切换到另一条声音，工作台不会再显示上一条的错误文案。
- 系统级“音频组件不可用”仍会在所有工作台显示；单文件错误只在对应源重新打开时显示，重试成功后自动清除。
- 新增 URL 同源、跨源和系统级错误三态回归，覆盖工作台切换声音时的错误隔离。

### 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：9 项执行、1 项因当前系统具备变调组件而正常跳过、0 失败。
- 最终 Release App 已完成本地入口和 31,259 条资料库启动走查；变调工作台的 URL 同源、跨源与系统级错误显示策略由回归测试覆盖，正常音频工作台入口保持可用。
- 试听已停止，应用已退出，工作区恢复为“全部声音”；本批只进行只读界面走查，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 87 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `07b920e413858184887402ff870942c5e184e4c265f4d9794e85182bc2f0abd0`；DMG `42422d911395d3a8ebbdc0a824fed9d469e944a09b043db859569b868f8012bb`。

## Build 86 记录

### 本批完成

- 资料库备份导出、备份读取和导入前的恢复备份全部移到 `LibraryPersistenceWriter` actor；31,259 条索引的 JSON 编码、读盘和写盘不再阻塞主线程滚动、试听或窗口响应。
- 备份操作增加明确的进行中状态和重复点击保护；设置页与资料库体检页会禁用冲突按钮并显示“正在后台处理资料库备份…”，完成后沿用现有提示反馈。
- 导入流程仍先后台验证 JSON，再回到主线程请求确认；确认后后台写恢复备份，最后才替换索引，原始音频和当前 SQLite 在确认前保持不变。
- 持久化 actor 新增导出/导入回归路径，确保维护操作与普通索引写入共用同一串行边界。

### 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 最终 Release App 在真实 31,259 条资料库完成主界面和设置入口走查；应用菜单可访问“设置…”，本地资料库正常显示，主列表、播放器与拖出入口保持既有状态。
- 备份导出/导入的耗时编码、读取和写入已由 `LibraryPersistenceWriter` actor 串行承接；设置页与资料库体检页的备份按钮在传输期间禁用并显示后台处理提示，确认前不替换索引。
- 应用已退出，工作区恢复为“全部声音”；本批只进行只读界面走查，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 86 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `6ccb56ae21a8d7e4020edec71271b43d9039cc644c9accd9b99747cf2908fdf6`；DMG `589fc7f72c48f4259673090c36c8375f1147775cd2fa752b46c1b2cacc392e1c`。

## Build 85 记录

### 本批完成

- 搜索结果任务现在在创建时携带不可复用的语义操作令牌；清空搜索、快速换词或切换资料库范围时，旧的 SQLite 语义索引任务即使延迟启动也只能安全退出。
- 语义进度、匹配数量和“理解到”概念会在新结果请求开始时立即清空，不再让旧任务在新列表已经显示后回写搜索徽章或进度状态。
- 相似声音查询与普通搜索共用同一套失效边界，避免两个后台语义任务互相覆盖进度。
- 新增延迟启动旧搜索回归：先创建旧请求，再失效语义操作，旧请求完成后可见状态仍保持清空。

### 验证记录

- CoreTests：78/78 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 最终 Release App 在真实 31,259 条资料库完成语义走查：`雨夜远处汽车` 得到 502 条结果并显示“理解到：车辆引擎、环境、拟音、天气环境、远处”；快速换词到 `机械门` 得到 4,748 条结果；清空后回到 31,259 条，旧语义徽章、概念和进度均消失。
- 定向延迟启动回归证明：旧请求创建后再失效，旧 SQLite 搜索即使之后才开始也不能回写匹配数量、概念标题或进度。
- 搜索已清除，应用已退出，工作区恢复为“全部声音”；本批只进行只读搜索，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 85 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `29eba00c4c8edcdc68ef32187675646052fb81aa8e2c2df566ea7ca8437b9a88`；DMG `e148d00ac84f12955e51c4b9f8d234b0ca060204d3d0ae11b2f1ddd58d3fbffa`。

## Build 84 记录

### 本批完成

- 播放错误从无归属的全局字符串改为绑定具体声音 ID 的原子状态；文件打不开、播放器无法启动或播放中解码失败时，错误不会再错误覆盖另一条声音的卡片。
- 底部播放器只在当前呈现声音与错误所属声音一致时显示错误；选择其他正常声音后立即恢复文件名、格式和索引时长，重新选择失败声音时仍可看到对应失败原因并重试。
- 新增真实不存在文件的播放失败回归，以及错误同声音显示、跨声音隔离和空错误安全隐藏的策略回归。

### 验证记录

- CoreTests：77/77 通过；AudioTests：24/24 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 最终 Release App 在真实 31,259 条资料库完成本地入口、选择、试听、实时进度、停止与拖入 FCP 提示走查；5 秒 `【电脑】_Typing_On_Keyboard.wav` 正常显示 `0:00 / 0:05`，播放中列表进度从 0% 开始更新，停止后回到 0%，全程没有出现错误文案。
- 定向失败回归使用不存在的 WAV 验证：错误状态记录发起播放的声音 ID，切换其他声音时播放器错误策略返回空，不会把旧错误显示到新卡片。
- 试听已停止，应用已退出，工作区恢复为“全部声音”；本批只进行只读播放，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 84 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `1be4b1912298ee4d9a69916da79aa68b715685a9515465836d20e8559a644c2c`；DMG `86e888e0bcd35897caa551f5aa5d05996ea7825b3fe09ff9549b9188b6a87ec5`。

## Build 83 记录

### 本批完成

- 修复底部播放器长期优先旧解码器项目的问题：播放中仍显示真正正在出声的声音；暂停、停止或播放结束后选择另一条，会立即显示新选择的名称、格式、索引时长与拖拽对象。
- 运输控制只在卡片所示声音确实是已加载项目时启用；卡片切到尚未播放的新选择后，前后跳转、停止、A/B 和进度拖动不会再暗中操作旧声音。
- A/B 临时裁剪范围只允许附着到同一已加载声音；选择新声音但尚未播放时拖入 Final Cut Pro 必定提供完整原文件，不会误用上一段的循环区间。
- 新增三态策略回归：播放中优先已加载项、停止后优先当前选择、没有选择时保留已加载项，覆盖快速浏览与暂停续播场景。

### 验证记录

- CoreTests：77/77 通过；AudioTests：22/22 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 最终 Release App 在真实 31,259 条资料库完成状态切换走查：24 秒音效播放到 37% 时选择另一条 9 秒音效，底部卡片仍锁定真正正在播放的旧声音；点击停止后立即切换到新选择，并显示 `0:00 / 0:09`。
- 新选择尚未加载时，停止、A/B 起点与终点均保持禁用，拖拽卡片准确显示新文件名；证明旧声音的运输状态和 A/B 区间没有泄漏到新选择。
- 试听已停止，应用已退出，工作区恢复为“全部声音”；本批只进行只读播放，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 83 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `3751955dbf1f17dd6fefed3afa31a6c283abf1b2779e1f40b0cb4439704dedac`；DMG `51ac8b5345cd75a5e408f53bc859d9900e3595b7a7c4487a860679cd0f671fd8`。

## Build 82 记录

### 本批完成

- 底部播放器在“已选择、尚未播放”状态下改用 SQLite 索引里的时长显示 `0:00 / 实际总时长`，不再出现文件已选中却显示 `0:00 / 0:00` 的误导状态。
- 顶部禁用进度条的 VoiceOver 数值同步使用索引总时长；按播放后立即切换为解码器确认的精确时长和实时进度，拖动、A/B 与循环逻辑不变。
- 时长回退只读现有 `SoundItem` 元数据，不打开、预热或读取音频；新增已加载优先、未加载回退和异常数值归零测试。
- 重新核对 Final Cut Pro 12.3 隔离测试资源库，时间线仍保留 Build 57 已验证的列表行、播放器原文件和 3.1665 秒 A/B 片段；未重复写入测试片段，并恢复两款应用的窗口尺寸。

### 验证记录

- CoreTests：77/77 通过；AudioTests：21/21 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 新增播放器呈现回归确认：未加载音频时索引时长 6.4 秒会直接呈现；加载后以解码器 6.25 秒为准；无穷大等异常索引值安全归零。
- 最终 Release App 在真实 31,259 条资料库选择未播放的 `1_大鼓.mp3` 后，底部卡片与禁用进度条立即显示 `0:00 / 0:02`；按播放后列表行出现实时百分比，停止后仍准确回到 `0:00 / 0:02`。
- 试听已停止，应用已退出，工作区恢复为“全部声音”；本批只进行只读播放，没有移动、改名、复制或写入原始音频。
- Release App 与 Build 82 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `5aa029a0e505a57b5af3276a92991621930a2563131967b6e2c1f4f72ad4eedc`；DMG `12f51249007715778e6c5279c4196cb8360ad56cb07a2717e004183ba2249658`。

## Build 81 记录

### 本批完成

- 资料库总声音数改为结构变化时一次重建的 O(1) 缓存；侧栏“全部声音”不再在播放进度、扫描状态或其他界面重绘时遍历所有音效包求和。
- 声音理解中心在逐条分析、频繁刷新进度时直接读取缓存总数，避免每个进度帧重复扫描完整资料库；匿名诊断导出也复用同一准确口径。
- 新增 SQLite 启动与结构删除回归，确认缓存能从三条索引恢复，并在移除音效包后立即归零；缓存只统计索引，不读取原始音频。

### 验证记录

- CoreTests：77/77 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 新增临时 SQLite 结构回归：启动后 O(1) 总数缓存为 3，调用真实移除音效包入口后立即归零，内存索引与持久化结构保持一致。
- 最终 Release App 在真实资料库启动后，侧栏“全部声音”和主标题均稳定显示 31,259；声音理解中心显示“全部资料库 1 / 31,259、当前结果 1 / 31,259”，证明进度面板复用了准确缓存口径。
- 声音理解面板空闲时进程 CPU 取样为 0.0%；本批没有启动声音分析，退出后工作区恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- Release App 与 Build 81 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `4f4c474f1996d7c05c37ba24c6a4a6dc01ba393422e2829f7ba542e718e3517c`；DMG `c57f50b4a1b02258e3f258e8810e5e146001436612ed0e6dd083aef997a0a628`。

## Build 80 记录

### 本批完成

- 删除 `LibraryStore` 中已无生产调用的同步 `visibleSounds` 与 `folderNodes` 旧接口，杜绝未来界面误用时在主线程重新展开、筛选并排序整套 31,259 条资料库。
- 原目录与搜索回归改为直接覆盖当前后台 `SoundResultsSnapshot` 管线；测试验证父目录包含后代、文字搜索和最终结果，不再以淘汰的主线程实现作为正确性基准。
- 结果请求与快照只放宽到模块内部可测试性，不增加对外 API；现有 UI 仍通过可取消后台任务生成有界分页结果。

### 验证记录

- CoreTests：76/76 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 新回归直接调用后台结果快照，确认“环境”父目录准确包含两个后代，并能进一步把“雨声”搜索收窄到唯一文件；测试不创建或读取任何音频内容。
- 最终 Release App 在真实 31,259 条资料库中完成“全部声音 → 音频库 → 爆炸_Explosions → Explosion 搜索”走查：整包显示 31,259 条、文件夹显示 6 条、搜索仍显示对应 6 条，列表和原生侧栏选择同步更新。
- 搜索已清除，工作区恢复为“全部声音”后退出；原始音频未移动、改名、复制、分析或写入。
- Release App 与 Build 80 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `0db70cf289a279630a1aea89ef20542123a40dc76c83de530f0538cfabdbf5c2`；DMG `ce0bd00f8c10fc2fe2d0ce177b1acc9c27e83ca7000bbea9224e35f0c7775200`。

## Build 79 记录

### 本批完成

- 侧栏改为 macOS 原生单选列表语义：全部声音、收藏和最近导入会暴露准确的 `selected / selectable` 状态，辅助操作点击行即可切换范围，不再只获得焦点。
- 智能分类、时长分段、音效包和多级原目录都绑定稳定的 `LibraryScope` 标签；切换后主标题、数量和分页结果与选中行同步，可展开节点继续保留系统展开/折叠操作。
- 移除没有穿透 SwiftUI `List` 包装层的自定义动作闭包，只保留实机已证明有效的系统选择机制，避免重复语义和额外行闭包。

### 验证记录

- CoreTests：76/76 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 最终 Release App 在真实 31,259 条资料库中完成原生侧栏选择走查：“全部声音”“最近导入”“音频库”和“爆炸_Explosions”均准确暴露 `selected` 状态；右侧标题、结果数量和 6 条文件夹结果同步切换。
- 总库切换仍维持有界分页，辅助功能树仅实例化当前可见的 10 行；退出后工作区已恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- 一次自动化读取曾短暂返回“无窗口”，应用重开后未复现，系统诊断目录也没有生成崩溃报告；第二次读取确认音效包范围已选中并显示 31,259 条结果，未将控制服务断连误记为应用崩溃。
- Release App 与 Build 79 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `8b718773b5d8a7b040240402dd0f276f18658fc6f0239ffbb83d0c9daeaf2bbc`；DMG `ecbf4a6cb65bec5ecfb026f9fd5de69388749ea08922a455b125b0006d1c689b`。

## Build 78 记录

### 本批完成

- 结果快照按范围惰性取材：最近导入、单个音效包和原目录不再先展开完整资料库，再丢弃无关声音；多音效包大库切换局部范围时减少一次整库数组的内存峰值。
- 31,259 条结果的 ID → 行号映射改为可取消的直接字典填充，不再由 `enumerated().map` 先构造同等规模的临时元组数组。
- 新增 31,259 条结果索引规模门禁，核对首行、中间行、末行和总数量，并限制纯内存构建在 1 秒内完成。

### 验证记录

- CoreTests：76/76 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 永久规模门禁用 31,259 条纯内存声音直接建立 ID → 行号字典，本机单项测试 0.082 秒完成；首行、第 15,629 行、末行和总数量全部准确。
- 真实 31,259 条资料库只读启动后，全部声音、分页列表、波形行和后台智能分类计数正常；自动化侧栏行本批只获得焦点、未代替用户完成范围切换，因此不把它记录成已完成的鼠标范围验收。
- 退出后工作区已恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- Release App 与 Build 78 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `f0cf05c13fde7d64605f4eec872250db2b468e7d28d9ffeac88ea6a790fb17d8`；DMG `2d712d6a6fbab92982594f430d83b9396f6e87954a5ec3da1bd38432e8f6cad4`。

## Build 77 记录

### 本批完成

- 点击“分析全部资料库”不再在主线程 `flatMap + filter` 完整音效库；Build 77 会立即显示准备状态，并在可取消的后台任务中直接按音效包挑出未隐藏、尚无音色指纹的候选。
- 后台队列构建只生成最终候选数组，不再额外生成一份 31,259 条整库中间数组；取消准备会同步取消后台工作并清理进度，不会误启动分析。
- “分析当前结果”和“分析全部资料库”共用同一候选规则；新增跨音效包回归，确认已隐藏和已有指纹的声音不会进入队列。

### 验证记录

- CoreTests：75/75 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 永久规模门禁用 31,259 条纯内存索引构建候选：本机单项测试 0.080 秒完成，隐藏项、已有指纹项和最终候选数全部准确；测试不创建、打开或读取任何音频文件。
- 在真实 31,259 条资料库中只读打开声音理解中心，覆盖数、分析当前/全部入口和本地只读说明均正常；为避免自动化等待期间开始写入生产索引，本批没有点击真实“分析全部资料库”。
- 退出后工作区已恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- Release App 与 Build 77 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `b3714f4f769f34ad46605a845405a8071a7121c2106461067f689efb8368a29b`；DMG `bfe7136a13837e9ec8bd740116b63fe466878a1a3d837c02a54453fb9bbcd437`。

## Build 76 记录

### 本批完成

- 资料库现在增量维护隐藏声音 ID 集合；“恢复全部隐藏项”直接处理隐藏子集，不再为少量隐藏项先 `flatMap`、过滤和复制 31,259 条完整资料库。
- 隐藏、恢复、持久化失败回滚和资料库结构重建统一由同一 ID 集合校准计数，避免数量缓存和真实可见性在异常路径中漂移。
- 电影感图标徽章统一声明为纯装饰；声音理解、相似声音、重复项、命名、交付检查和工作台等标题不再让 VoiceOver 额外朗读 SF Symbol 的英文系统名称。
- 隐藏项回归改为走真实的“一键恢复”入口，并验证提示、计数与 SQLite 最终状态。

### 验证记录

- CoreTests：73/73 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 临时 SQLite 回归从两条隐藏记录启动，经真实“一键恢复”入口后确认提示为“已恢复 2 个隐藏声音”、内存计数归零且最终持久化全部可见；测试数据和原始音频完全隔离。
- 在真实 31,259 条资料库中打开“声音理解中心”和 400 候选的“相似声音与变体”，辅助功能树只保留中文标题、操作和候选信息，未再出现 `Ecg`、`Waveform Path` 等 SF Symbol 英文朗读。
- 真实资料库当前 0 个隐藏项，因此未改动生产索引来制造恢复场景；退出后工作区已恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- Release App 与 Build 76 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `2c921c134eef542172254e0573b279c89bcbcb28303e27c2426b214b25b24049`；DMG `dcb53c85a88dbeecdadfd5ff8686a0810bdb28937474a411c7e299e4536ea89d`。

## Build 75 记录

### 本批完成

- “已隐藏重复项”从按需遍历每个音效包/声音改为 `LibraryStore` 的 O(1) 缓存；资料库加载和结构重建一次校准，隐藏、恢复与保存失败回滚则按实际变化条数增减。
- 主界面更多菜单和重复项处理中心在播放进度、滚动、筛选或其他重绘期间不再为了判断“恢复全部隐藏声音”入口而扫描数万条记录。
- 新增启动恢复、一条隐藏、两条恢复和 SQLite 最终一致性回归，确保性能优化不会让计数、可见性或本地索引漂移；原始音频继续只读。

### 验证记录

- CoreTests：73/73 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 新增 SQLite 回归确认隐藏数量能在启动时一次恢复，并随单条隐藏、批量恢复和最终持久化准确变化；菜单热路径只读取缓存，不再遍历音效包或声音数组。
- 在真实 31,259 条资料库中完成实机走查：静止及播放进度更新期间反复打开“更多操作”均立即出现；0 个隐藏项时只显示“显示已隐藏的重复项”，未误显示“恢复全部隐藏声音”。
- 播放并打开菜单后的进程抽样 CPU 为 0.2%；退出后工作区已恢复为“全部声音”，原始音频未移动、改名、复制或写入。
- Release App 与 Build 75 DMG 已完成签名、版本一致性和 SHA-256 门禁：App `4cc9f8761b4da9449e9de44f97853ca66fda837f991869437416ef880f32e31f`；DMG `122706a70629bf7728b332f156a30424a022e7ae75262473e9750a2e013ec843`。

## Build 74 记录

### 本批完成

- 全库音色指纹覆盖数改为 `LibraryStore` 增量维护：单条分析、12 条分批发布、源文件变化失效、资料库替换和结构重建都会同步校准，不再由界面每次重绘遍历全部分析记录。
- 声音理解中心不再在每次进度更新时 `flatMap` 整个资料库或扫描当前数万条结果；全库总数只按音效包计数，当前结果覆盖率只在面板打开、单条分析完成或批量分析结束时后台计算。
- 修复批量声音理解存在失败时提示原样显示 `(failed)` 的文字错误，改为准确插入失败条数；新增 SQLite 启动覆盖数与当前范围后台计数回归。

### 验证记录

- CoreTests：72/72 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 新增 SQLite 回归确认 App 启动时能把已有指纹覆盖数一次恢复为缓存值，并能在后台准确计算“当前结果”子集；无指纹条目不会误计。
- Build 74 正式 App 在真实 31,259 条“全部声音”范围打开声音理解中心，包含菜单操作和完整界面读取约 2.5 秒，稳定显示“全部资料库 0 / 31,259、当前结果 0 / 31,259”；未启动分析，关闭后主界面完整恢复。
- 面板空闲时进程 CPU 为 0.0%，证明覆盖数字呈现后没有持续遍历或后台轮询；本批未读取任何声音内容，也未修改原始素材。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`01fc1cab654ec4bb0e92ac6c912cece0b4602f794395cb92ce599c56b41b1713`。
- DMG SHA-256：`c816678577bdcb0ca6ac1f81def262165c1213164eb3551c26654529f29ef764`。
- 可回退归档：完成本批文档提交后生成 `versions/v0.5.0-build74-cached-acoustic-coverage`。

## Build 73 记录

### 本批完成

- 素材库体检的“已检查、缺失文件、疑似重复、波形缓存”四张指标卡分别合并为单个 VoiceOver 焦点，直接读出中文指标名与数值；警告卡额外提示“需要关注”。
- 体检标题听诊器和文件状态图标退出焦点顺序；缺失文件行改为“缺失文件：名称 + 音效包/相对路径”，可定位文件行的访达按钮则保留独立、带名称的动作。
- 视觉布局、只读检查、后台快照、取消/重开隔离和重复项计算均保持不变。

### 验证记录

- CoreTests：71/71 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- Build 73 正式 App 在真实 31,259 条资料库中打开体检时，检查中页面只保留标题、动作、进度和状态文本，Build 72 的独立“听诊器”图像焦点已经消失。
- 完整体检结果页把四张卡稳定读成“已检查 / 31,259”“缺失文件 / 0”“疑似重复 / 121，需要关注”“波形缓存 / 1 MB”；不再产生 `waveform`、`Question Mark On A Folder`、`square.on.square` 或 `externaldrive` 独立图标节点。
- 结果仍为缺失 0、疑似重复 121 项 / 53 组、复用 5,417 个缓存指纹；数据库、恢复备份、缓存维护和重复项处理入口全部保留。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`6a30293a52f747031ab57452e58cbd8904cfd2c22a009dd4d5aac9afbcc741fd`。
- DMG SHA-256：`30641932ca0d53acd649621da6c2848d2117bbb93ebcf29a4d2c50184208b6c6`。
- 可回退归档：完成本批文档提交后生成 `versions/v0.5.0-build73-health-result-semantics`。

## Build 72 记录

### 本批完成

- 素材库体检不再在打开面板的主线程上同步复制数万条声音、拼接 URL 与读取缓存体积；快照改为后台 utility 任务并每 512 条响应取消，面板可以先立即呈现检查状态。
- 重新检查、资料库变化或关闭窗口会取消旧快照/检查；所有完成、失败与取消回写都核对本轮 ID，较晚结束的旧任务不能把新一轮进度隐藏或用旧报告覆盖新报告。
- 新增快照构建回归，确认安全作用域重定位根目录、显示名、相对路径、文件事实、数据库体积和恢复备份状态完整传入体检，原始音频仍只读。

### 验证记录

- CoreTests：71/71 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- Build 72 正式 App 在真实 31,259 条资料库中两次打开体检面板，包含桌面控制与完整状态读取的端到端时间约 1.0–1.2 秒；首帧已分别显示约 4,056 / 31,259 与 4,368 / 31,259，证明快照和文件核对没有阻塞面板呈现。
- 在第二轮约 4,680 / 31,259 时关闭体检，约 2 秒内完成关闭、取消和完整主界面无障碍树恢复；随即重开从新的约 4,680 / 31,259 进度开始，没有闪回旧报告或被旧取消回写隐藏。
- 新一轮随后完整检查 31,259 条，报告缺失 0、疑似重复 121 项 / 53 组、复用 5,417 个缓存指纹；资料路径、SQLite/恢复备份与波形缓存结果均正常，原始声音保持只读。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`fd5f0b50f823472068aac9580518c5c4d8e439b048c03472aa6c242b9569a1c7`。
- DMG SHA-256：`96842b5c13bbbbf9923e3a9b99da0ab5407b556bdac3725a2054577d02f07325`。
- 可回退归档：完成本批文档提交后生成 `versions/v0.5.0-build72-background-health-snapshot`。

## Build 71 记录

### 本批完成

- 侧栏标签把图标、标题、数量和状态合并为一个稳定焦点；VoiceOver 直接读出“名称，数量个声音”或“名称，正在计算”，不再逐个停在 SF Symbol 图标上。
- 音效包离线与自动刷新状态继续通过整行提示传达，隐藏装饰图标不会丢失业务信息；侧栏视觉、选择、展开和大目录惰性物化保持不变。

### 验证记录

- CoreTests：70/70 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- Build 71 正式 App 刚进入 31,259 条真实资料库时，八个智能集合分别只产生一个“名称，正在计算”焦点与中文提示；Build 70 中单独出现的 `Wind`、`Starburst Shape, Filled`、`Shoe Prints, Filled`、`Cpu Chip, Filled` 等图标节点全部消失。
- 分类数量完成后，父类、子类和叶子行均以单个可选择行读出名称与准确数量，例如“转场与呼啸，6,562 个声音”“其他转场，3,419 个声音”；展开/折叠动作、层级和选择状态仍由系统保留。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`98744242e477a59ad621334b07a1878f202c32a9984332a0dd3bc6a6da89cfd1`。
- DMG SHA-256：`ef9932bf5aa02e96ef11c95a5cb3d5b06cc724c6f83e0f74752493ecf11eaa07`。
- 可回退归档：完成本批文档提交后生成 `versions/v0.5.0-build71-consolidated-sidebar-semantics`。

## Build 70 记录

### 本批完成

- 搜索方式、组合筛选、自定义集合、格式筛选、排序方式和更多菜单新增稳定的中文 VoiceOver 名称，不再依赖 SF Symbol 的系统英文描述或只靠帮助文字区分两个“过滤”。
- 刷新音效包、资料库体检、批量选择、导入音效包和声音工作台在宽/窄两种命令栏布局下共享同一语义，图标收缩时仍能明确读出动作。
- 播放器的后退/前进 5 秒、播放/暂停、停止、A/B 起终点、清除片段、循环和试听队列补齐动态中文标签；当前声音拖出入口合并为单个焦点并提供 Final Cut Pro 拖拽提示。
- 资料库标题徽章、扫描结果图标、列表表头拖移符号、拖出角标和音量右端装饰改为无障碍隐藏；视觉完全保留，但不再产生 `Hdr`、`Up-Right Diagonal Arrow`、`高音量` 等无意义独立节点。

### 验证记录

- CoreTests：70/70 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 在 Build 70 正式 App 的真实无障碍树中，命令栏稳定读出“搜索方式：仅匹配原文”“组合筛选”“自定义集合”“格式筛选”“排序方式：文件名”“刷新音效包”“资料库体检”“批量选择”“导入音效包”“声音工作台”和“更多操作”；两个筛选菜单不再同名。
- 未选择声音时，播放器明确读出后退/前进 5 秒、播放、停止、A/B 起终点、循环、空队列与“未选择可拖出的声音”；选择真实 WAV 后拖出入口即时变为“拖出当前声音”，并保留拖入 Final Cut Pro 的提示。
- 完整界面对照未再发现独立的 `Hdr`、`Up-Right Diagonal Arrow`、`高音量` 或“拖移”图像节点；装饰图标继续可见但已退出 VoiceOver 焦点顺序。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`052a0b2fe1b946f53353e2a577a978a1ce531603d4b8f309222f406fe24ac77a`。
- DMG SHA-256：`13b2d2509f723fda46e37750c56a9d9df5aaeb4ecca0a8f62db40efabed8da82`。
- 可回退归档：完成本批文档提交后生成 `versions/v0.5.0-build70-clear-accessibility-semantics`。

## Build 69 记录

### 本批完成

- 音效包 `DisclosureGroup` 只有在该包实际展开时才创建首层文件夹行；折叠包不再把完整目录树交给 SwiftUI 和无障碍系统。
- 每个有后代的 `FolderTreeRow` 同样只在本层展开时创建下一层，递归目录改为逐层物化；一个代表数千条声音的折叠分支只保留一行和准确后代计数。
- 深层文件夹工作现场恢复仍按现有范围规则自动展开所选路径祖先，用户主动展开/折叠和文件夹筛选语义不变；优化不裁剪、不合并也不改写原目录结构。
- Build 68 对照基线：真实 31,259 条资料库从折叠状态展开音效包约 8 秒回到空闲、物理内存约 200 → 231 MB；展开后的完整无障碍读取额外约 11 秒回到空闲、约 217 → 229 MB。

### 验证记录

- CoreTests：70/70 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 在当前真实 31,259 条资料库上从折叠状态展开整个音效包，仍约 8 秒回到空闲；物理内存约 216 → 236 → 216 MB。Build 68 同动作约 200 → 231 → 218 MB，说明本批把临时增量从约 31 MB 降到约 20 MB，并消除了这轮展开后的约 18 MB 残留，但不宣称首层时间改善。
- 展开后的完整无障碍读取本轮约 13 秒回到空闲、Build 68 对照约 11 秒，存在运行波动，尚无延迟改善证据；本批结论限定为折叠分支的视图/内存边界收紧。
- 将工作现场恢复为 10,110 条的 `Adobe音效库` 文件夹后，约 3.6 秒进入对应范围；音效包与 Adobe 层自动展开，29 个直接子目录可访问，其中“噪音音调DTMF测试”的后代仍保持折叠且未递归暴露，声音列表只创建首个 100 条窗口。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`03146202682c30f3035e5a54f3a129878f463e7f382f1c7580b9d07e4bec6d34`。
- DMG SHA-256：`5a08341e28d9ae4032248b9fb71cb28f1775273a38f65c297502ef106788bc4d`。
- 可回退归档：`versions/v0.5.0-build69-lazy-sidebar-branches`。

## Build 68 记录

### 本批完成

- 悬停预听门控新增“交互阻断”状态：扫描开始立即取消当前行的延迟预听并清除已武装状态，扫描遮罩期间的移动和离开事件都不能重新武装。
- 扫描结束后门控再次从静止参考点开始，只有鼠标真实移动至少 3 点才允许新的悬停预听；遮罩消失或无障碍检查不会让指针下方声音意外起播。
- 新增纯状态回归，覆盖“已经武装 → 扫描阻断 → 遮罩期间移动/离开 → 扫描结束 → 再次真实移动”的完整顺序；同时保留列表换页/重建后的静止指针保护。

### 验证记录

- CoreTests：70/70 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用当前真实 31,259 条资料库，在播放器卡片显示 `低沉氛围_(1).wav` 且未播放的基线上完成全复用刷新；界面报告读取 0，暖缓存下约 4 秒回到 0% CPU，列表锚点继续停在第 115 条附近。
- 扫描完成后播放器卡片仍为 `低沉氛围_(1).wav`，快退、快进、停止和 A/B 控件继续禁用，列表没有出现活动进度行；Build 67 验收中由遮罩切换后悬停触发的其他声音未再出现。
- 门控状态回归确认扫描阻断期间的移动/离开均不会武装预听，扫描结束后第一次位置采样仍保持安静，只有后续至少 3 点真实移动才重新允许悬停。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`bfb88c0d5c7fde93d752e1f140942a18e1a0f3e1828191a09a968bb1cd5ad7ba`。
- DMG SHA-256：`88e0594aebcdd114bbac0bbaf55bb8122f5f421780e9968ffcffd81820b4124b`。
- 可回退归档：`versions/v0.5.0-build68-hover-rearm-after-scan`。

## Build 67 记录

### 本批完成

- 全复用刷新不再在目录枚举前为全部旧声音建立内容指纹字典；只有出现无法按原路径匹配的新候选时，才启动改名解析。
- 改名解析只收集本次候选真正请求的指纹，并跳过已被原路径认领的旧身份；稳定 31,259 条资料库无需创建整库指纹键、数组和哈希桶。
- 修正重复素材的身份延续：两个内容和文件事实相同的声音中，一个留在原路径、另一个改名或移动时，移动的副本现在可以唯一匹配尚未被占用的旧身份。
- 新增重复副本局部改名回归，验证两条自定义名称和 UUID 分别保留、只报告一条路径迁移，测试前后源字节不变；原有多候选歧义保护继续拒绝猜测。

### 验证记录

- CoreTests：69/69 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用当前真实 31,259 条资料库完成全复用刷新，界面报告“索引已是最新 · 复用 31,259 条”，读取、新增、更新、移除均为 0；资料库范围、当前选择和第 115 条附近的列表锚点保持。
- 同一测试方法下，扫描阶段从约 257 MB 物理内存开始、峰值约 259 MB，约 19 秒进入空闲；Build 66 对照为约 275 MB 开始、峰值约 364 MB、约 34 秒完成。完整无障碍树读取后的进程物理内存约 287 MB，CPU 回到 0%。
- 重复副本局部改名回归确认：原路径先认领自己的身份，移动副本唯一继承未被占用的 UUID、自定义名称和迁移记录；两个测试文件的源字节均保持不变。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`c1c4c91398df800671ef1243f442ad3d39ddc32648b8b594f079431a2233736c`。
- DMG SHA-256：`e448027fec7ad6eae485bce8d25dcf674ceb229ca5b46af1321e088516d466ce`。
- 可回退归档：`versions/v0.5.0-build67-lazy-rename-fingerprints`。

## Build 66 记录

### 本批完成

- 扫描器在候选数量与已有索引一致、每条路径和文件事实都可复用且不存在路径迁移时，直接返回共享的 `existingItems` 快照。
- 全复用路径不再启动元数据任务组、逐条构造新 `SoundItem`、按路径重新排序、建立两套 UUID 集合或计算增删差集；只保留目录枚举与轻量事实核对。
- 快速路径仍发布准确的发现/复用/读取/增删改统计和首尾进度；任一候选新增、变化、缺失或改名都会自动回到完整扫描，不降低正确性。
- 扩展扫描进度回归测试：首次扫描继续提供有界进度，全复用二次扫描必须精确报告 `0 → 总数`，并返回与已有索引完全一致的条目。

### 验证记录

- CoreTests：68/68 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用当前真实 31,259 条资料库完成两次全复用刷新，界面均报告“索引已是最新 · 复用 31,259 条”，读取、新增、更新、移除均为 0；当前选中的 `低沉氛围_(1).wav` 保持不变，列表可见锚点继续停在第 115 条附近。
- 第二次受控采样从约 275 MB 物理内存开始，刷新峰值约 364 MB，约 34 秒完成，完成瞬间约 346 MB；共享快照消除了后半段重复构造声音数组的工作，但目录枚举、候选事实数组和提前建立的旧索引指纹表仍是主要时间/内存成本，已明确进入下一批继续压缩。
- 全复用进度回归精确报告 `0 → 总数`；任一新增、变化、移除或改名仍回到完整扫描路径，源音频继续不移动、不改名、不复制、不写入。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`463bba8b6a675d3f4ea79acdc463fa0674178dc55266ed08c9c6df83f0ed5ec0`。
- DMG SHA-256：`9ae9ca3592aeed59f67c8c8b71d87b9bcef93896d5d4589c8846639e7a3a8834`。

## Build 65 记录

### 本批完成

- 手动刷新在统计确认没有新增、更新、移除或路径迁移后立即短路，不再替换当前音效包条目、重建查找表、增加资料库/分类修订或安排 36,487 行 SQLite 保存。
- 自动文件夹监控使用同一结构变化判断；外接盘时间戳只有数值漂移时，不再因为新扫描数组的 `Date` 位模式不同而误触发整库持久化。
- 全复用刷新保留当前声音、滚动所依赖的列表身份和上一次实际索引更新时间；仍发布本次轻量扫描统计，让界面可以明确显示“复用全部、读取 0”。
- 扫描完成横幅从完整 `LibraryScanResult` 改为不含声音条目的 `LibraryScanReport`，只保存计数、路径迁移和完成时间，不再长期留住第二份全库数组。
- 新增真实 `LibraryStore` 无变化刷新回归：在故障注入的不可写数据库下确认不触发保存、无错误提示、修订与选择不变且源字节只读。

### 验证记录

- CoreTests：68/68 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 首次真实刷新时，外接盘上名为“待删除”的 5,228 条目录已不再存在；拾响正确报告移除 5,228、复用 31,259、读取 0 并更新索引。该变化来自源目录外部状态，拾响未删除任何原文件。
- 在新的真实 31,259 条基线上重启后再次刷新，界面报告“索引已是最新 · 复用 31,259 条”，读取、新增、更新、移除均为 0；当前选中的 `低沉氛围_(1).wav` 保持不变，列表可见锚点继续停在第 115 条附近。
- 全复用刷新前进程物理内存约 240 MB、历史峰值约 327 MB；刷新峰值约 363 MB，完成后约 295–317 MB，约 2 秒后 CPU 回到 0%。Build 64 同类全复用刷新峰值约 468 MB，且之后仍有持续整库保存/分类 CPU。
- 故障注入回归将测试数据库替换为不可写目录，确认全复用刷新不会安排持久化、不会报错，也不改变资料库修订、分类修订、选择、条目或源字节。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`33374a7c5057ac3386643e7f23bde67c86f3df7cb6b331ff47c514cc8600960d`。
- DMG SHA-256：`381251443d2fdaefb0ea41300e4cac575c768f032474b8877019a71b08d8a48d`。
- 可回退归档：`versions/v0.5.0-build65-unchanged-refresh-shortcut`。

## Build 64 记录

### 本批完成

- 增量扫描的修改时间判断新增极窄容差：只接受 Foundation 日期与 SQLite Unix 时间往返产生的 1 微秒以内数值漂移，避免未变化的外接盘文件被重复打开。
- 文件大小仍需完全一致；1 毫秒及以上的真实时间变化继续触发内容签名和音频元数据检查，不会用性能优化掩盖实际更新。
- 素材库体检复用相同时间策略，亚微秒漂移时直接使用已有快速内容签名，不再对无变化文件做额外只读抽样。
- 新增分数秒文件时间经 SQLite 保存、加载和再次扫描的端到端回归测试，以及容差边界测试；测试确认零读取复用且源字节不变。

### 验证记录

- CoreTests：67/67 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 在 Build 63 已写入当前索引的同一真实 36,487 条外接盘资料库上刷新，界面明确报告“索引已是最新 · 复用 36,487 条”，新增、更新、移除和读取均为 0；此前重复出现的 14,004 条误判完全消失。
- 刷新前新进程物理内存约 285 MB、历史峰值约 320 MB；全复用刷新与后续保存/分类活动最高约 468 MB，完成后稳定约 369 MB，CPU 回到 0%。
- 真实验证同时确认下一层优化空间：扫描虽已零读取，但当前刷新完成路径仍保存整套 SQLite 索引并重建派生状态；该无变化短路进入 Build 65，不影响本批时间戳修复的正确性。
- 分数秒文件 SQLite 往返回归测试和 1 微秒/1 毫秒边界测试均通过；临时源文件字节保持不变，原始音频继续只读。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`c46cee005c95978fadf355c504ab3b43a66bc239f9779148e3745d92e08bb790`。
- DMG SHA-256：`5103f94b4ae7de3665ad9b3e4ab1de5954e22f62b769efd09c1a606850eb717d`。
- 可回退归档：`versions/v0.5.0-build64-zero-read-refresh`。

## Build 63 记录

### 本批完成

- 全库扫描候选不再为每个文件保存一份完整旧条目，只保留指向单一 `existingItems` 快照的整数索引；按路径和内容指纹查找也共享同一份条目数据。
- 改名匹配直接在候选数组原位完成，移除首次写入时产生的整数组复制；智能分类索引改为逐包遍历，移除 `flatMap` 创建的全库临时条目数组。
- 智能分类文本缓存增加 50,000 条与约 24 MB 双重上限，并按键长和分类结果估算成本；长期浏览不同资料库时可由系统逐出旧分类结果。
- 已有声音发生变化但暂时无法被系统解码时，继续保留上一次确认过的时长、采样率和声道信息，同时更新只读检测到的文件大小、时间与快速签名；新出现的不可读文件仍以未知音频信息安全入库。
- 新增不可读替换文件的元数据保留、二次增量复用、源字节不变及创作者规模分类缓存预算测试；原始音频继续不移动、不改名、不复制、不写入。

### 验证记录

- CoreTests：65/65 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条外接盘资料库完成两次全量刷新；第一次读取 14,004 个被判定为变化的文件并复用 22,483 条，条目总数保持 36,487，刷新、SQLite 保存和智能分类重建结束后 CPU 回到 0%。
- 第一次刷新前进程物理内存约 235 MB、历史峰值约 301 MB；整个重刷新最高约 495 MB，完成后稳定约 347 MB。第二个全新进程从约 219 MB 开始，同类刷新峰值约 482 MB、完成后约 334 MB；相较 Build 62 变化扫描曾观察到的约 1.3 GB 高水位明显下降。
- 第二次刷新仍报告相同的 14,004 条变化；只读比对确认所有 36,487 个文件均存在且大小一致，部分外接盘修改时间在 SQLite 往返后仅相差约 0.000000238 秒。该精度误判已记录为 Build 64 的下一项优化，不归因于源文件变化。
- 不可读替换回归测试确认已知时长、采样率、声道、收藏、别名和标签均保留，二次扫描可复用；测试前后临时源文件字节完全一致。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`35d7a2cc3a99c14608a5ed9483b4e88a53731964dacd188bfe5ce809d3d69268`。
- DMG SHA-256：`c6d281fdf84f8cbecdee6fe3020a85673d153f9e5b0a683cf48811301e31ab8e`。
- 可回退归档：`versions/v0.5.0-build63-bounded-library-scan`。

## Build 62 记录

### 本批完成

- 单个别名或标签修改新增独立元数据修订与精确变化 ID；普通时长、原目录和格式排序下只替换发生变化的行，不再重建、筛选和排序整套 36,487 条结果。
- 结果失效改为依赖感知：名称排序、当前搜索和智能分类仍会在元数据影响成员或顺序时正确重算；普通全部、收藏、最近导入、音效包和原目录范围不再订阅无关分类修订。
- 批量标签、智能中文命名及撤销统一发布精确元数据变化；视图可局部更新行内容，同时智能分类索引仍按分类修订失效。
- 单条别名/标签和直接追加标签补齐 SQLite 保存失败回滚；只有当前界面仍对应失败写入时才恢复旧值，不覆盖更新较新的编辑。
- 恢复快照改为元数据 SQLite 写入成功后再安排；失败状态不会被错误写入备份，无实际变化的重复保存也不会产生修订和磁盘任务。
- 新增元数据依赖策略、精确修订、无操作保护、单条失败回滚和多行标签失败回滚测试；原文件名、目录和音频内容继续只读。

### 验证记录

- CoreTests：63/63 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库切换到“时长排序”，选中 `02_黑马_1_Dark_Tower.mp3` 后将主列表滚动条设置到 `0.08`；保存临时别名与两个标签时，无障碍树只更新声音工作台字段，主列表节点、滚动值和当前选择均未重建。
- 同一声音随后通过拾响界面恢复原状态；SQLite 最终核验 `custom_name = nil`、`tags_json = []`、`favorite = 0`，并已恢复“全部声音 + 文件名排序 + 未选择声音”的干净工作区。
- 故障注入将临时测试数据库路径替换为不可打开的目录，确认单条别名/标签与两行直接标签在写入失败后都恢复旧值；测试只使用临时目录。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`fc91a55593ee9f1d8537a228907f242be86d6c00013416760e29a8e63e16fcea`。
- DMG SHA-256：`4aed1914fca2aa43406f50acdd70bfa8ae8c40c390c9905d0ca6fb595d7611a8`。
- 可回退归档：`versions/v0.5.0-build62-local-metadata-edits`。

## Build 61 记录

### 本批完成

- 可见行音频预热从单一“最多 12 个播放器”升级为数量与 48 MB 估算驻留成本双重预算；估算综合时长、采样率、声道数和源文件大小。
- 超过单项预算的超长/超大音频不再后台预解码，点击时仍可正常按需播放；短音效继续享受最多 12 条的即时起播缓存。
- 缓存插入后若合计超出预算，会按最近使用顺序释放最旧播放器；取出播放、替换、清空时同步维护精确估算值，不产生负数或悬空计数。
- 新增 macOS 内存压力响应：系统发出 warning/critical 信号时主动停止并释放预热播放器，不影响当前正在播放的主播放器。
- 智能分类成员索引只缓存最新一代；标签或命名修改后，旧版全库 UUID 集合不再最多保留三份，进行中的调用仍持有自己的安全快照。
- 新增解码成本估算、超预算拒绝、真实播放器按预算淘汰回归测试；原始音频继续只读。

### 验证记录

- CoreTests：61/61 通过；AudioTests：20/20 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库完成四页连续滚动：稳定后的物理内存从约 224.3 MB 增至约 248.8 MB，增量约 24.5 MB，峰值约 282.6 MB；同类 Build 60 重操作曾达到约 368 MB 物理占用。
- 四页滚动后列表滚动值到达 `0.6813145492830642`，可见行全部保持“尚未播放”，进程空闲 CPU 为 0.0%，界面持续可交互。
- 额外检索并播放时长 1:00:00 的 `逐渐增强.mp3`，确认超过 48 MB 预热预算的声音仍能按需打开、播放和立即停止；停止后物理内存稳定在约 249.4 MB，原文件未被修改。
- 验证后已清除临时搜索与选择，恢复“全部声音 + 文件名排序 + 未选择声音”的干净工作区。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`3f09a6f760626cd9f228da950fb22c03addcc885f980c119129c56b7355a0300`。
- DMG SHA-256：`15119a35348cad444ebc6df79563e3cd1a56bc122908f4e74ea4dae428fce71b`。
- 可回退归档：`versions/v0.5.0-build61-bounded-preview-memory`。

## Build 60 记录

### 本批完成

- 普通音效列表的收藏操作升级为 O(1) 局部刷新：只更新发生变化的行，不再重建、筛选和排序整套 36,487 条结果，也不改变当前滚动位置。
- 侧栏收藏数量改为随索引维护的常量时间计数；单个或批量收藏无需再次扫描全库，结构性导入、移除和刷新仍会重建准确总数。
- 只有“收藏”范围或“仅收藏”筛选会因星标变化重新计算成员；智能分类、音效包、文件夹和普通搜索保持当前列表身份与波形缓存。
- 单个及直接批量收藏补齐 SQLite 保存失败回滚：只有当前状态仍对应失败操作时才撤回，避免快速连续点击时覆盖更新较新的选择。
- 收藏恢复快照改为数据库写入成功后再安排，失败状态不会被错误写进备份；新增局部失效策略、计数、持久化与故障回滚回归测试。

### 验证记录

- CoreTests：61/61 通过；AudioTests：18/18 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库在首屏与非零滚动位置各完成一组收藏/取消收藏闭环；四次键盘动作分别约 9 ms、6 ms、11 ms、8 ms，侧栏收藏数均即时在 1 与 2 之间同步。
- 中段测试前后列表保持同一批 100 个可访问节点、可见范围保持 37–48、滚动值精确保持 `0.05309562051813557`；选中声音与播放器停止状态均未改变，证明没有整表重建或意外试听。
- SQLite 最终核验两个临时测试声音均恢复为未收藏，全库收藏总数恢复为 1；验证后已重启并恢复“全部声音 + 文件名排序 + 未选择声音”的干净工作区。
- 保存失败回归测试将临时测试数据库路径替换为不可打开的目录，确认 UI 乐观更新后会自动恢复星标、收藏总数与修订状态；测试只操作临时目录。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`adf01421d0c8df6f91c3b627d4056bdaaa11a1e75992127d325e25f7679379e6`。
- DMG SHA-256：`479ccea8029061afdb2f89efa6baff454f7a5dccf9db0e12148cb9256fb96a7d`。
- 可回退归档：`versions/v0.5.0-build60-instant-favorites`。

## Build 59 记录

### 本批完成

- 创作现场恢复扩展到“上次选中的声音”：只保存当前范围内的 UUID，重启后自动选中并滚动定位，不保存播放进度、不会自动起播。
- 恢复深层智能分类、音效包或原目录时，侧栏会自动展开父级；原目录只展开所选路径的祖先，不扰动其他目录。
- 智能分类数量后台计算期间统一显示“—”，并暂不展示会误导用户的空子层级；真实数量就绪后一次替换，避免大型资料库启动时闪现 0。
- Build 58 工作区 JSON 不含选中声音时仍可直接读取，新增字段保持向后兼容；失效声音不会产生空白选择或错误滚动。
- 大库分页升级为最多 720 行的双向滑动窗口；跨重启或键盘定位到任意远端声音时只创建目标附近 240 行，避免为滚动定位一次性实例化前方数万条记录。

### 验证记录

- CoreTests：59/59 通过；AudioTests：18/18 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库恢复第 3 万条附近的声音 `动物_Animal_Mammal_Herbivore_Panda_Bear_Bark_02.wav`：约 2.5 秒进入本地界面，声音正确选中且播放器保持停止，没有自动起播。
- 同一恢复场景的无障碍列表只创建目标附近 240 条声音与两个分页入口；连续滚动触发第二页后为 480 条声音与两个入口，进程空闲 CPU 从修复前约 98% 降到 0.0%–0.1%，界面持续可响应。
- 智能分类真实数量一次呈现，未闪现误导性的 0；转场与呼啸 8,047、环境与氛围 10,312、音乐与旋律 12,463 等大类均完成后台计数替换。
- 验证后已恢复“全部声音 + 文件名排序 + 未选择声音”的干净工作区，并重新启动最终 App 留作使用。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`01d43e4e585c0e659d79720df6cc4ea8a2ba2e7378ffb43e6cd833cbb92e01ad`。
- DMG SHA-256：`f65c15d9beade667096b522395beca772c55f5368aed44fefb86a13cf6f18dec`。
- 可回退归档：`versions/v0.5.0-build59-calm-continuity`。

## Build 58 记录

### 本批完成

- 新增创作现场恢复：关闭再打开本地资料库后恢复上次使用的音效包、原目录或智能集合，以及文件名/时长/原目录/格式排序；临时搜索词和筛选条件保持一次性，不制造意外结果。
- 工作区状态使用带版本号的本地 JSON 保存；音效包被移除或原目录消失时会自动退回“全部声音”，不会启动到失效或空白范围。
- 试听音量、主播放进度和变调试听进度补齐 VoiceOver 名称、百分比/时间读数和调整提示；开启“减少动态效果”时，命令按钮保留清晰按压反馈但不再缩放。
- 资料库或结果集刚出现时，悬停预听会等待真实鼠标移动后再启用，避免窗口切换或列表重建时由静止指针意外触发播放。
- DMG 打包适配新版 macOS：优先使用 `diskutil image` 生成压缩 UDZO，保留 `hdiutil create` 和 HFS+ hybrid 两级兼容回退。
- 版本归档新增 `.DS_Store` 与 AppleDouble 元数据排除，后续回退包只保留可复现源码、当前 App、当前 DMG、校验值和提交记录。

### 验证记录

- CoreTests：57/57 通过；AudioTests：18/18 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库完成最终 Build 58 启动回归：从模式页进入资料库后，可见声音全部保持“尚未播放”，播放器处于停止禁用态；静止指针不再触发悬停试听。
- 将工作区设为“收藏 + 时长排序”后完整退出并重启，界面恢复到收藏范围且显示唯一收藏声音；验证后已恢复“全部声音 + 文件名排序”。
- 无障碍树确认主播放进度显示“播放进度、0:00 / 总时长、上下调整播放位置”，音量显示“试听音量、85%”；变调进度使用同一语义化控件并通过 Release 编译。
- `run-product-checks.sh`、Release App、本地签名、README/RELEASE_NOTES 版本一致性和 SHA-256 门禁全部通过；`diskutil image info` 确认最终 DMG 为 UDZO/zlib 只读压缩镜像，新打包路径没有旧 `hdiutil create` 弃用警告。
- App 可执行文件 SHA-256：`662a6ab659dd543e4938e16d50b5f379893a27a81d3d244471af81cc4cd9a7bf`。
- DMG SHA-256：`00604497121d9331a8ce6d41a36ef4cb6ad7df97b8ed850c5306eeabf0664e7b`。
- 可回退归档：`versions/v0.5.0-build58-continuity-accessibility`。

## Build 57 记录

### 本批完成

- 新增真实声学相似度：本地提取 12 段频谱、明暗、滚降、平坦度、过零率、峰均比、瞬态密度和动态范围，跨文件名与目录检索最近音色；SQLite 只保存不可还原音频的小型指纹。
- 新增“声音理解中心”：可对当前结果或全部资料库建立音色指纹，任务低优先级、逐条持久化、支持取消续跑，并按 12 条一批刷新界面以避免大库重绘。
- 新增 5 条快速对比试听：候选可即时切换、播放、淘汰、收藏、保留和拖入 Final Cut Pro；响度拉齐只对较响项衰减，退出后恢复普通播放音量。
- 创作者搜索新增镜头意图、情绪和明确时长解析，可理解推进/拉远/切镜/慢镜/落版，以及悬疑、紧张、温暖、浪漫、史诗、空灵和“两秒以内、2–5 秒”等表达。
- 新增 Jacksun 命名字典与模板：自定义素材词、个人叫法、前缀、分隔符、编号保留和同名冲突编号；配置进入 SQLite 与恢复快照，原始音频和文件名始终只读。
- 新增 FSEvents 音效包自动监控：递归感知新增、删除与替换，防抖后后台增量刷新；前台播放、批量操作和声音分析拥有更高优先级，外接盘离线/恢复状态可见。

### 验证记录

- CoreTests：53/53 通过；AudioTests：18/18 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库完成 Build 57 启动、搜索和滚动走查；资料库包含 123,410 条语义概念索引与 31,349 条相似家族记录。“紧张推进两秒以内”被解析为“≤ 2 秒、紧张、强烈、上升转场、转场”，返回 55 条结果；连续滚动 5 页的输入动作约 159 ms，界面保持可响应。
- 完成声音理解中心、真实声学相似度、5 条快速对比、Jacksun 命名字典和自动监控入口走查；未启动全库声学分析、未应用命名建议，也未写入任何原始音频。
- macOS 27.0、Final Cut Pro 12.3 上使用 `/private/tmp/Shixiang-Smoke-Build55.fcpbundle` 隔离资源库完成真实时间线烟测：列表行与播放器卡片原文件拖入成功；A/B 片段以 `02_基础上升音效_2_02_Essential_Riser_2-clip-BE3725E1` 可读名称进入时间线，时长 3 秒 9 帧，导出文件为 3.1665 秒、48 kHz 双声道 AAC。
- 快速连续点击三段声音后，仅最后一段保持播放状态；前两段立即恢复“尚未播放”。源 WAV 在烟测前后大小、修改时间和 SHA-256 完全一致，确认路径、文件名和内容未变化。
- Release App、UDZO DMG、本地签名、README/RELEASE_NOTES 版本一致性及 App/DMG SHA-256 门禁全部通过。
- App 可执行文件 SHA-256：`bb3fd850e0cbadd6936b612744d744ebfc1b6ccda282f4268633459ad24d9456`。
- DMG SHA-256：`f420c9c108bfadba8c4b4052c6be57acf93a383424860586201ddbd2cf822578`。
- 可回退归档：`versions/v0.5.0-build57-creator-intelligence`。

## Build 56 记录

### 本批完成

- 新增完全本地的中英语义搜索：自然语言意图与 FTS/CJK 原文结果混合排序，搜索菜单可随时切换为仅匹配原文。
- 新增 SQLite 持久语义索引和旧资料库懒回填进度；索引只保存声音概念与变体家族，不包含音频字节，也不访问网络。
- 新增“相似声音与变体”：优先聚合同目录基础命名相同的编号、远近和强弱版本，再按语义、时长、BPM 与调性排序，结果维持有限分页并可直接试听、拖入 Final Cut Pro。
- 新增智能中文命名审核：展示原名、建议别名、标签、置信度和识别理由，支持大库压缩全选、低置信度/已有别名边界控制和后台批量写入。
- 智能命名只修改拾响 SQLite 中的别名和标签，原始文件名与音频保持只读；最近八次撤销记录保存在本机，菜单可安全撤销上一次仍未被手动改动的批次。

### 验证记录

- CoreTests：50/50 通过；覆盖跨语言搜索、旧库语义索引懒回填、命名变体家族、中文别名清理、撤销记录持久化，以及智能命名从 Store 到 SQLite 的应用/撤销闭环。
- AudioTests：15/15 通过；KeyAnalyzerTests：5/5 通过；PitchShiftTests：8 项执行、1 项因当前系统具备对应变调组件而正常跳过、0 失败。
- 使用真实 36,487 条资料库的一致性副本完成性能验证：首次本地语义索引约 26.9 秒，之后自然语言热搜索约 40 ms，相似候选约 66 ms；相似候选上限为 400，首屏保持 120 条懒加载。
- 在最终 Build 56 App 中输入“雨夜远处汽车”，从真实资料库得到 536 条本地语义结果；索引过程中主界面持续显示进度，完成后展示理解到的概念。
- 实际打开相似声音和智能中文命名审核界面；混合中英文件名会去除素材编号与重复英文尾巴。仅完成预览，未应用任何命名建议，未改动原始音频。
- Release 构建、UDZO DMG、本地签名、README/RELEASE_NOTES 版本一致性与 App/DMG SHA-256 门禁：全部通过。
- App 可执行文件 SHA-256：`ee63762e4aa207554d34ed67b17eb63a0eab9b849c6e29cf571f4d7d9e0aee4b`。
- DMG SHA-256：`c58c7208bce83cc1c5d40a80e97858c237d93872f50b35dc863426a620b7ba77`。

## Build 55 记录

### 本批完成

- 几万条批量收藏和标签写入改为后台 SQLite 事务，显示准备/保存/应用进度并支持取消；取消会完整回滚，不留下半批数据。
- 重复音效处理中心支持搜索、试听、选择保留项、忽略重复组、从主列表隐藏其余项和一键恢复；所有处理只发生在拾响索引层。
- 资料库体检优先复用增量扫描保存的文件大小、修改时间和快速内容签名，只有无法复用时才重新读取抽样。
- Final Cut Pro 交付诊断新增源文件、UTI、时长、声道、采样率检查，并提供原文件或 A/B 片段的真实时间线拖拽测试卡。
- 隐藏项、忽略重复组会进入 SQLite 与恢复快照；主列表可以临时显示隐藏项，原始音频始终保持只读。

### 验证记录

- CoreTests：43/43 通过。
- AudioTests：15/15 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、1 项按当前系统具备变调组件而跳过、0 失败。
- 使用真实 36,487 条资料库完成体检和重复处理中心界面验证：0 个缺失项，识别 3,044 个疑似重复项、2,581 组；处理入口保持分页和懒加载，未对原始文件执行写入。
- macOS 27.0、Final Cut Pro 12.3 上对 0.62 秒、48,000 Hz 双声道 WAV 完成自动交付诊断，源文件、UTI、时长、声道/采样率和拖拽表示全部通过；隔离测试资源库已关闭，真实时间线三类手工拖拽仍按烟测清单待确认。
- Release 构建、DMG 打包、本地签名、App/DMG SHA-256 发布检查：全部通过。
- App 可执行文件 SHA-256：`bcf27a61ca15755fa0d41bf7d82d5d381929a1842f16ffeb377267d68b66e3a6`。
- DMG SHA-256：`36f3c9479daf65b7ae50bbec05bcafa5d8212dff6730e34c0a25cfd08eeb3c02`。

## Build 54 记录

### 本批完成

- 增量扫描会复用未变化文件的音频元数据，只打开新增或变化文件，并在界面汇报新增、更新、移除、复用和读取数量。
- 重新定位增加错误文件夹保护；没有匹配到原资料库声音时拒绝替换索引，原有收藏、别名和标签保持不变。
- 素材库体检新增分阶段进度、取消操作、按音效包归组的缺失项，以及就地增量刷新/重新定位入口。
- 主列表新增批量模式，支持选择当前全部结果、批量收藏/取消收藏和追加标签；3 万条全选使用压缩选择状态，批量写入合并为 SQLite 事务。
- 新增首次使用引导、可执行空状态、`⌘R` 增量刷新、`⌘⇧H` 资料库体检和更明确的操作反馈。

### 验证记录

- CoreTests：39/39 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、1 项按当前系统具备变调组件而跳过、0 失败。
- 使用真实 36,487 条资料库完成批量全选和素材库体检界面验证；全选不再创建数万个选中对象。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过。
- App 可执行文件 SHA-256：`49cdf0da5e2861d02c257d0332b4fb92ad8693c9ec13c289ac27112d80d5fe4b`。
- DMG SHA-256：`b137df769ca8596efe8803c06872f915cf59bd32aad902b2e41ebedba4dd493d`。

## Build 53 记录

- 新增自动更新清单契约与 HTTPS 地址校验；当前本地版默认不配置更新源、不访问网络。
- 设置页可以检查更新并在发现新版本时打开下载页，正式售卖版可按同一接口接入更新服务。

## 验证记录

- CoreTests：36/36 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；内置隐私资源检查也通过。

## Build 52 记录

- 新增私有发证脚本 `scripts/issue-license.sh`，私钥只从环境变量读取，不进入仓库、App 或诊断包。
- 正式售卖版现在具备“发证 → 粘贴 → 本机公钥验证 → 保存/移除”的完整离线流程；当前本地版仍不要求激活。

## 验证记录

- CoreTests：35/35 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；内置隐私资源检查也通过。

## Build 51 记录

- 设置页新增离线许可证粘贴、验证、保存和移除入口；当前本地版不要求激活。
- 正式售卖版只需配置公钥和发证流程即可启用，不上传音频、不改动本地资料库。

## Build 50 记录

- 新增离线许可证契约与 Ed25519 公钥验证基础；当前本地版不要求激活。
- 设置页展示授权状态，正式售卖版可在不上传音频的前提下接入签名许可证。

## Build 49 记录

- 隐私说明随 App 打包，并可直接从“支持与更新”页查看。
- Release 门禁新增内置隐私资源检查，避免交付包缺少本地版隐私承诺。

## 验证记录

- CoreTests：32/32 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；内置隐私资源检查也通过。

## Build 48 记录

- 新增“支持与更新”设置页，可导出匿名诊断包，帮助售后定位扫描、播放和 Final Cut Pro 拖拽问题。
- 新增 `Docs/PRIVACY.md`，明确本地版、诊断包和未来联网版的数据边界。
- 保持本地优先：诊断包只包含版本、系统和数量统计，不上传音频、不读取文件名和路径。

## 验证记录

- CoreTests：32/32 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 47 记录

- 新增产品准备度矩阵，逐项记录本地功能、自动化证据、真实桌面烟测和正式售卖外部条件。
- 明确临时签名本地版与正式上架版本的边界，避免把未公证包宣传为最终商业包。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 46 记录

- 空资料库页面新增 Finder 文件夹拖放导入，保留原有目录与文件名，只建立本地索引。
- 拖放导入复用可取消扫描任务和进度反馈，失败或取消不会替换当前资料库。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 45 记录

- 新增 A/B provider 重复 `public.file-url` 请求回归测试，确保同一次拖拽协商不会生成多个临时片段。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：14/14 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 44 记录

- A/B 拖拽的具体音频 UTI 与 `public.file-url` 表示共享同一个懒导出任务，避免系统协商期间生成重复临时片段。
- A/B 导出失败或取消时删除未完成的临时文件，保持拾响自己的拖拽缓存干净。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 43 记录

- 新增 `scripts/release-preflight.sh`，正式售卖前检查 Developer ID Application 证书、公证 Profile 和当前交付包。
- 新增 `Docs/RELEASE_READINESS.md`，明确区分本地可运行版本、真实 Final Cut Pro 烟测和上架售卖的外部前置条件。
- 前置脚本不会读取或打印私钥、Profile 内容或用户音效。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 42 记录

- 发布检查新增 README / RELEASE_NOTES / App Build 编号一致性门禁，版本说明错配时拒绝交付。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过；README / RELEASE_NOTES 版本一致性门禁也通过。

## Build 41 记录

- 导入/重新定位任务保留可取消句柄，扫描遮罩提供“取消扫描”入口。
- 取消扫描被视为正常结束，不再把 `CancellationError` 显示成失败提示；未完成扫描不会写入新索引。
- 取消后的延迟进度回调会被 token 丢弃，避免覆盖下一次扫描状态。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过。

## Build 40 记录

- 音效包扫描支持节流进度回调，导入和重新定位时显示已读取数量与总数。
- 扫描状态使用 token 隔离；旧任务完成后的延迟回调不会覆盖新一次导入的 UI 状态。
- 新增扫描进度回归测试，确认进度从 0 开始、以总文件数为分母并在完成时收敛到总数。

## 验证记录

- CoreTests：31/31 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过。

## Build 39 记录

- 分类索引缓存键加入音效包指纹，避免同一进程内多个资料库复用错误的智能分类结果。

## 验证记录

- CoreTests：30/30 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、3 项按当前受限音频环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过。

## Build 38 记录

- 播放波形的实时高亮改为异步 Canvas 绘制，拖动定位仍保持同步 1:1 反馈。
- 鼠标进入列表行时提前预热音频解码器，快速切换相邻声音时降低起播空档；悬停预听等待缩短为 260ms。
- 播放音量本地记忆，音量图标支持一键静音与恢复上次音量。
- 智能分类按 `classificationRevision` 构建共享 ID 索引，侧栏计数和大集合筛选复用索引，避免重复分类整库。
- 新增智能分类索引回归测试，确认父级、子级、时长叶节点和“未识别”分支一致。

## 验证记录

- CoreTests：30/30 通过。
- AudioTests：13/13 通过。
- KeyAnalyzerTests：5/5 通过。
- PitchShiftTests：8 项执行、1 项按环境跳过、0 失败。
- Release 构建、DMG 打包、签名、App/DMG SHA-256 发布检查：全部通过。

## Build 37 记录

- 检查日志：七步产品检查的阶段编号统一为 `[1/7]` 到 `[7/7]`，交付记录不再产生歧义。

### 验证记录

- Build 37 完整产品检查：待执行。

## Build 36 记录

- 变调稳定性：启动前探测系统变调/播放器组件；组件缺失时工作台优雅降级，PitchShiftTests 记录环境跳过而不崩溃。
- 测试门禁：产品检查新增 PitchShiftTests，正常桌面音频环境会执行完整变调、时长、原子替换和取消用例。

### 验证记录

- Build 36 本机验证：PitchShiftTests 8 项执行、3 项因系统组件缺失跳过、0 失败；Core/Audio/KeyAnalyzer 与 Release 构建此前通过。

## Build 35 记录

- 发布门禁：`release-check.sh` 在 DMG 存在时核对当前版本 DMG 的 SHA-256，App 与交付包必须一致。

### 验证记录

- Build 35 完整产品检查：CoreTests 30/30、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包、App 签名和 DMG SHA-256 门禁全部通过。

## Build 34 记录

- 备份导入安全：拒绝不安全相对路径、控制字符和异常音频元数据；替换资料库前重置播放器并清理可重建缓存。
- 结果取消：全库结果展开、分类筛选、格式/时长/收藏过滤与查询命中循环均支持细粒度取消。

### 验证记录

- Build 34 完整产品检查：CoreTests 30/30、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。

## Build 33 记录

- 归档隔离：源码备份排除项目内历史 `versions/`，避免新归档递归包含旧版本，长期维护保持可控体积。

### 验证记录

- Build 33 完整产品检查：CoreTests 29/29、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。

## Build 32 记录

- 归档瘦身：版本备份不再重复复制历代 DMG，只保留当前 Build 的 App、DMG/SHA-256、源码和校验清单。

### 验证记录

- Build 32 完整产品检查：CoreTests 29/29、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。

## Build 31 记录

- 搜索取消：快速连续搜索或切换筛选时主动取消旧的全库结果任务，避免过期查询继续占用 CPU。

### 验证记录

- Build 31 完整产品检查：CoreTests 29/29、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。

## Build 30 记录

- 分类一致性：智能分类缓存键加入音效包内相对路径和格式，改名或移动后自动得到新分类；新增回归测试。

### 验证记录

- Build 30 完整产品检查：CoreTests 29/29、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。

## Build 29 记录

- 大库滚动稳定性：筛选、收藏或搜索更新时保留原生 `List` 与可见行身份，只重置 240 条分页窗口，减少波形和解码预热的重复创建。
- 产品检查输出：统一显示六步检查编号，避免交付日志产生歧义。

### 验证记录

- Build 29 完整产品检查：CoreTests 28/28、AudioTests 13/13、KeyAnalyzerTests 5/5、Release 构建、DMG 打包和签名门禁全部通过。
- 当前受限执行环境没有可用的 AVAudioPlayerNode 音频组件；PitchShiftTests 的离线渲染用例仍需在正常桌面音频环境中单独复核，不影响应用编译与交付包检查。

## Build 28 记录

- 发布构建稳定性：Swift Release 构建使用独立临时 scratch，并显式关闭 dSYM 生成，修复 Xcode 27 下 `GenerateDSYMFile … Operation not permitted`。
- DMG 兼容性：当受限环境无法创建 UDZO 时，自动回退为可挂载的 HFS+ hybrid DMG，仍生成 SHA-256。
- 版本可回退：Build 27 基线归档保留在 `versions/v0.5.0-build27-baseline-1243fdb`；本批 Build 28 交付归档会写入同一 `versions/` 目录，原始音频未复制、未移动、未改名。

## Build 27 记录

- DMG 交付包：新增 `scripts/package-dmg.sh`，生成带 Applications 快捷方式、使用说明和 SHA-256 的可分享安装包。
- 产品检查：`scripts/run-product-checks.sh` 现在会同时生成 DMG，再执行 Release 签名门禁。
- 外部边界：DMG 仍使用当前配置的签名身份；Apple 公证需另行运行 `scripts/notarize-app.sh`。

## 验证记录

- Build 26 功能测试：CoreTests 28 项、AudioTests 13 项、KeyAnalyzerTests 5 项通过。
- Release build：待本批完整检查。

## Build 26 记录

### 本批完成

- 产品验证命令：新增 `scripts/run-product-checks.sh`，统一运行 Core、Audio、KeyAnalyzer、Release build 和 release check。
- Final Cut Pro 手工烟测：新增 `Docs/FINAL_CUT_PRO_SMOKE_TEST.md`，覆盖列表行、播放器图标、A/B 片段、快速切换、滚动性能和源文件只读核对。
- 验证边界：GUI 烟测仍需在真实桌面权限下执行，不把静态 provider 测试冒充成时间线实测。

### 验证记录

- Build 25 功能测试：CoreTests 28 项、AudioTests 13 项、KeyAnalyzerTests 5 项通过。
- Release build：待本批完整检查。

## Build 25 记录

### 本批完成

- 版本归档瘦身：新增 `scripts/create-version-backup.sh`，排除 SwiftPM 缓存、构建 scratch、Git 内部目录和重复的 dist 源副本。
- 回退证据不减少：每份新归档仍包含源码、可运行应用、Git 提交号、SHA-256、版本号和原始音频只读声明。
- 安全边界：脚本拒绝覆盖已有归档，旧的 800MB 版本备份保留不动。

### 验证记录

- Build 24 功能测试：CoreTests 28 项、AudioTests 13 项、KeyAnalyzerTests 5 项通过。
- Release build：待本批完整打包验证。

## Build 24 记录

### 本批完成

- 大库中文搜索：新增 SQLite CJK 字符索引，中文查询改为索引命中，不再每次对完整 FTS 内容执行全表 `LIKE`。
- 兼容回填：旧数据库首次启动时自动建立字符索引；结构保存和别名/标签更新会同步维护索引。
- 大库验证：新增 6000 条音效的中文搜索测试，确认“啸”等中间字符能够稳定命中。

### 验证记录

- CoreTests：28 项通过。
- AudioTests：13 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 23 记录

### 本批完成

- 售卖发布准备：Developer ID 签名路径支持 runtime + timestamp。
- 公证脚本：新增 `scripts/notarize-app.sh`，读取用户配置的 `xcrun notarytool` Keychain Profile，完成提交、Staple、验证和签名复核。
- 当前机器未配置公证凭据，因此本批只验证脚本前置检查，不会向 Apple 提交任何内容。

### 验证记录

- CoreTests：27 项通过。
- AudioTests：13 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 22 记录

### 本批完成

- Final Cut Pro 拖拽：普通音频的 `public.file-url` 数据表示现在由拾响显式提供，新增测试实际读取文件 URL 数据。
- 发布签名：`scripts/build-app.sh` 支持 `SHIXIANG_CODESIGN_IDENTITY`，不配置时仍使用本机临时签名。
- 发布门禁：`scripts/release-check.sh` 会拒绝未提交的源码改动，再检查应用结构、版本号、SHA-256 和签名。

### 验证记录

- CoreTests：27 项通过。
- AudioTests：13 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 21 记录

### 本批完成

- 设置 / 关于窗口：提供版本信息、Jacksun 品牌信息、本地优先与隐私说明。
- 本地资料库维护：在设置中打开素材库体检、导出备份、清理波形缓存和释放预加载声音。
- 发布检查脚本：`scripts/release-check.sh` 自动检查应用结构、版本号、可执行文件 SHA-256 和代码签名。

### 验证记录

- Debug build：通过。
- Release build：待本批完整打包验证。

## Build 20 记录

### 本批完成

- 资料库恢复闭环：支持从 JSON 备份导入索引、我的集合和本地分析结果。
- 安全替换：导入前先自动写入当前恢复备份，再替换拾响本地索引；原始音频始终只读。
- 备份校验：拒绝损坏 JSON、重复音效包 ID、重复声音 ID 和错误的包归属关系。
- 体检反馈：导入成功后自动重新检查资料库，失败原因通过应用内提示展示。

### 验证记录

- CoreTests：27 项通过。
- AudioTests：12 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 19 记录

### 本批完成

- 资料库备份导出：在“素材库体检 → 本地数据库”中导出 JSON，包含音效包索引、集合和 BPM/LUFS/调性结果，不复制原始音频。
- 导出过程使用原子写入；目标目录不存在时自动创建，失败会回到应用内提示，不影响当前播放和扫描。

### 验证记录

- CoreTests：26 项通过。
- AudioTests：12 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 18 记录

### 本批完成

- 路径迁移可见化：扫描确认的改名、移动和改名并移动会在主界面提示，可展开查看旧路径、新路径、类型和时间。
- 迁移提示可隐藏；隐藏只针对本次扫描结果，下一次新的迁移仍会再次提醒。
- 详情列表限制为前 200 条，避免异常大型迁移记录撑满界面；完整数量仍保留在提示中。

### 验证记录

- Debug build：通过。
- Release build：待本批完整打包验证。

## Build 17 记录

### 本批完成

- 我的集合：支持重命名、删除和范围可用性提示；失效的音效包/文件夹会明确显示，不会伪装成空集合。
- 扫描结果：记录唯一指纹确认的改名、移动和改名并移动，保留音效 ID 与旧/新相对路径，便于复核索引迁移。
- 波形缓存：后台按 90 天 TTL 和 256MB 容量预算清理旧缓存；统计、清理与内存释放都在 utility 任务中执行。
- Final Cut Pro：普通原文件和 A/B 裁剪片段均提供 `public.file-url` 文件表示，并保留具体音频类型协商。

### 验证记录

- CoreTests：25 项通过。
- AudioTests：12 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：待本批完整打包验证。

## Build 16 记录

### 本批完成

- 源文件指纹：记录大小、修改时间和快速内容签名；原路径优先，改名/移动后只有唯一匹配才继承原 ID、收藏、别名和标签。
- 重复文件安全：同一旧指纹出现多个新候选时不自动迁移，避免复制文件抢走原条目元数据。
- 智能信息与波形缓存：源指纹变化时智能信息会失效，波形缓存身份同步变化，避免沿用旧分析和旧波形。
- SQLite 迁移：旧数据库启动时自动补齐源指纹列，旧 JSON/旧恢复数组仍可读取。
- 扫描安全：文件系统枚举遇到权限错误时中止本次扫描并给出明确提示，不会静默缩小资料库。

## 验证记录

- CoreTests：23 项通过。
- AudioTests：11 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：`scripts/build-app.sh` 成功，应用签名校验通过。

---

## Build 14 记录

## 本批完成

- 中文连续词搜索：对 CJK 查询补充参数化字符级匹配，避免“呼啸”中间字无法搜到。
- 我的集合：保存并恢复全部、最近导入、音效包、文件夹和智能目录范围。
- 恢复快照：SQLite 损坏恢复时保留集合、收藏/别名/标签及 BPM、近似 LUFS、调性智能信息；兼容 Build 10–13 的旧数组格式。
- 数据一致性：别名与标签更新在同一 SQLite 事务内同步主表和 FTS；结构保存会清理已删除声音的智能记录。
- 大库性能：结果计算响应取消；预加载器使用有界 worker 队列、token 和 generation 取消；数据集切换会作废离屏预加载。
- 播放流畅度：静态波形路径缓存，播放波形和进度条刷新上限为 60Hz。
- Final Cut Pro：A/B 临时拖入片段超过 7 天或超过 128 个时自动清理。
- 安全：移除音效包前增加确认，只移除索引，不删除原始音频。

## 验证记录

- CoreTests：20 项通过。
- AudioTests：11 项通过。
- KeyAnalyzerTests：5 项通过。
- Release build：`scripts/build-app.sh` 成功，应用签名校验通过。
- PitchShift 的 AVAudioUnit 离线渲染 XCTest 在当前无音频组件的测试环境中仍无法运行；不影响本批代码编译和应用打包。
